# 룰 체크: 11(어택) / 12(블록) / 13(시큐리티 체크) / 14(배틀) / 17(룰체크) / 18(기타)

Ver.4.2 대비 엔진(state.js, engine.js, main.js `attackFlow` 계열, effects.js) 검증 결과. 상태: **수정됨** / **열림** / 정상.

| 룰 번호 | 룰 내용 | 엔진 동작 | 상태 |
|---|---|---|---|
| 1-2-3-1 / 11-5-1-4 / 11-2-7-4 | 어택 중인 디지몬이 배틀 에어리어에 없으면 어택은 어디에도 성립하지 않는다 | 카운터/【어택 시】 효과로 어택 디지몬이 사라져도 `resolveSecurityCheck`가 시큐리티 0이면 어택측 승리 처리(재현 확인). 그 외에도 시큐리티 카드를 그대로 공개함 | 수정됨 (stepSecurityCheck: 어택 스택 없으면 i=0에서도 중단, main.js resolveFinalTarget/enterBlockCheck 가드) |
| 11-2-6 / 11-2-7-4 | 대상 디지몬이 사라져도 어택은 그 대상인 채 어디에도 성립하지 않고 종료 | 배틀 직전 대상/어택자가 사라지면 `resolveDigimonBattle`이 null → UI가 `res.attackerCardId`에서 TypeError | 수정됨 (resolveFinalTarget + 배틀 콜백에서 null이면 endAttack) |
| 1-2-3-1 / 11-5-1-2-1 / 1-3-4 | 체크 가능한 수가 0(《S어택-1》)인 디지몬은 시큐리티 0인 상대에게도 승리하지 못하고, 0회 처리는 못한다 | total<=0이어도 시큐리티 0이면 승리/1장 체크 진행 (재현) | 수정됨 (다른 에이전트와 동시 수정, total<=0이면 체크 없음) |
| 11-2-1 | 어택 선언은 턴 플레이어만 | `declareAttack`이 턴 플레이어 검사를 안 함(UI 드래그만 막음) | 수정됨 (activePlayer 검사) |
| 11-2-5 / 12-1-4 | 레스트할 수 없는 디지몬은 어택 선언/블록 불가 | 선언은 hookNoRest만 검사(`preventRest` 상태 무시), 블록은 레스트 실패해도 블록이 성립(restStack 실패 후에도 대상 변경) | 수정됨 (`canRestByRule` 신설: declareAttack과 eligibleBlockers에서 검사. 블록 레스트는 룰 처리이므로 효과 면역 무시) |
| 11-2-7-3 | 어택 대상을 이미 어택 대상인 것으로 바꿀 수 없다 | 대상 변경 옵션(“이 디지몬으로 변경”, 《돌진》, 플레이어로 변경)이 현재 대상과 같아도 제시됨 | 수정됨 (enterRedirectTiming에서 제외) |
| 13-1-8-2 → 13-1-8-3 | 체크로 공개된 카드의 【시큐리티】 효과를 **모두 해결한 뒤** 시큐리티 디지몬과 배틀 | 효과를 pending에 넣은 채로 즉시 DP 비교(효과로 DP 변화/어택 소멸/카드 이동해도 무시) | 수정됨 (beginSecurityCheck.deferBattle → reveal 후 대기, `battleSecurityCheck`에서 배틀. 일반 호출(soak)은 즉시 배틀 유지) |
| 13-1-8-3-2 / 14-2 | 시큐리티 “디지몬”이 없으면(옵션/테이머, 또는 효과로 이미 등장·패에 추가됨) 배틀 없이 다음으로 | 옵션/테이머도 DP 0으로 배틀 처리, 시큐리티 DP 보정이 옵션에도 가산될 수 있었음. 【시큐리티】로 등장/패 추가된 카드도 배틀 | 수정됨 (result `noBattle`; 트래시 내 카드 수 감소로 이탈 판정) |
| 13-1-8 / 15 | 【시큐리티】 “이 카드를 코스트를 지불하지 않고 등장시킨다” (331개 세그먼트) | `playFree zone:hand, filter:null`로 컴파일 → **핸드의 아무 카드나** 무료 등장, 정작 공개된 카드는 등장하지 않음 | 수정됨 (op `playThisFree`, `playThisFreeFromTrash`) |
| 14-2-5 / 13-1-8 | 【시큐리티】 “배틀 종료 시, …”(약 52개) 는 시큐리티 디지몬과의 배틀이 끝난 뒤 처리 | 위와 같은 오컴파일(패에서 등장)로 즉시/오작동 | 수정됨 (op `afterBattle` 보류 → battleSecurityCheck 끝에서 `queueAfterBattle`로 pending 등록) |
| 17-1-3-1-1 | 배틀 에어리어의 DP 0인 디지몬 소멸 | 구현됨. 단, 테이머/옵션(DP 없음)에 DP를 가감하면 DP 0으로 보고 소멸 | 수정됨 (ruleCheckDP가 테이머/옵션 제외) |
| 17-1-3-2-1 | DP를 가지지 않은 배틀 에어리어의 디지몬은 (소멸이 아닌) 파기 | BT18-086 같은 DP 없는 디지몬은 아무 처리 없음(다른 에이전트가 “2-5-3”으로 스킵만 추가) | 수정됨 (ruleCheckDP에서 파기, 소멸 이벤트 없음) |
| 17-1-3-2-5 | 링크 상한 초과 링크 카드 파기 | 룰체크 없음(≪링크+N≫ 진화원이 사라져도 링크 카드 잔존) | 수정됨 (ruleCheckDP에서 초과분 파기; 단 ruleCheckDP가 호출되는 시점에 한함) |
| 17-1-3-2-6 / 17-1-3-2-7 | 링크 조건에 맞지 않는 링크 카드 / 〈링크〉 주석 외 카테고리 링크 파기 | 링크 조건 재검사 없음(grantedBy가 호출부마다 불일치해 신뢰 불가) | 수정됨 (`ruleCheckDP`가 링크 카드의 링크 조건 술어(`parseLinkGrant`+`evoTargetPredicate`)와 디지몬 카테고리를 재검사해 파기; 조건이 술어로 파싱되는 카드만 대상) |
| 17-1-3-2-2 / -3 / -4 | 배틀 에어리어의 옵션(룰상 놓인 것), 육성 에어리어의 비디지몬, 뒷면 카드 파기 | 해당 룰체크 스윕 없음(뒷면 카드는 모델링 자체가 없음) | 정상 (구조적으로 발생 불가: 옵션은 효과(`placeThisInBattle`)로만 배틀 에어리어에 놓이고 육성 에어리어는 디지타마/디지몬 경로만 존재 — 둘 다 룰의 예외 「효과로 놓인 것」이며 뒷면 카드는 모델 없음) |
| 17-1-2-1 / 17-1-2-2 | 룰/효과 처리 중에는 룰체크가 행해지지 않는다(예: DP-3000 후 《S어택-1》 부여 뒤에 소멸) | `modifyDP`가 즉시 `ruleCheckDP`로 소멸시킴(효과 처리 중간). 효과 후반 처리(“DP를 깎고 ~를 준다”)가 대상 소멸로 누락될 수 있음 | 수정됨 (`runScript` 깊이 카운터 `state._rcDepth` > 0 동안 `ruleCheckDP`는 보류, 최외곽 효과 종료 시 `flushRuleChecks`; 효과 처리 중 DP 0 디지몬은 효과 끝까지 유지) |
| 17-1-3 (동시) / 15-4-3-3 | 룰체크로 소멸한 카드의 【소멸 시】는 그 시점의 발동 대기 효과와 동시 유발 | 즉시 삭제 후 pending에 추가되어 순서 선택이 되지만 “동시 처리” 보장은 없음 | 수정됨 (`flushRuleChecks`가 만든 pending에 `rcSim` 표시 → 파생 트리거(depth+1)가 아니라 대기 중 효과와 같은 티어에서 순서 선택) |
| 18-1-1 / 18-1-2 | 보류 처리(“이 턴 종료 시 메모리-3”)는 해당 타이밍에 유발과 같이 처리, 동시면 동시 유발 순서 | `runEndOfTurnEffects`가 fn을 endTurn 맨 앞에서 즉시 실행(턴 종료 시 유발 효과와 순서 선택 불가) | 수정됨 (예약 효과는 `queueScheduledTurnEnd`로 pending에 올라 동시 유발과 함께 순서 선택. 단순 지속시간 만료는 `expire:true`로 확정 시점 처리) |
| 18-2 (置換) | 「대신에」 이후 처리로 치환, 강제/임의, 치환에는 즉시형 끼어들기 불가 | 카드별 스크립트/훅으로 개별 구현. 범용 엔진 없음 | 열림 (카드별 구현으로 커버) |
| 18-3 (永久循環) | 무한 루프 시 턴 플레이어/비턴 플레이어가 반복 횟수 선언, 정지 수단 없으면 무승부 | 미구현(루프 탐지 없음) | 수정됨 (휴리스틱: 한 턴에 유발 효과 1500회 초과 해결 시 정지 수단 없음으로 보고 `winner='draw'` 무승부, 18-3-2. 반복 횟수 선언/정지 수단 선택(18-3-3)은 미구현) |
| 11-1 / 11-3 / 11-4 / 11-5 | 어택 흐름: 선언 → 대상 변경 → 카운터 타이밍(1회) → 블록 타이밍 → 성립 확인 | 순서·1회 제한 정상 (카운터 1회, 블록 1회) | 정상 |
| 11-2-2 / 11-2-8-1 | 어택 선언과 대상 선택은 동시 | 드래그 드롭은 동시. 클릭식(대상 선택 창)에서는 【어택 시】가 먼저 pending에 쌓여 대상 선택 전에 처리 가능 | 수정됨 (`attackFlow`가 【어택 시】 유발/`attack` 이벤트를 대상 확정 후(`enterRedirectTiming` 진입 시)로 지연; 브라우저 검증: 대상 선택 창 동안 pending 0) |
| 11-2-3 / 11-2-4 | 1회 어택에 1체, 어택 중 새 선언 불가(≪진격≫ 포함) | `startAttack`은 `!sel.pendingAttack` 가드, 일반 UI는 blockIfBusy | 정상 (엔진 레벨 가드는 없음) |
| 11-2-7-1 | 대상은 상대 플레이어 또는 레스트 상태 상대 디지몬 | legalDigimonTargets 정상(테이머 제외, 예외 효과 별도) | 정상 |
| 11-2-7-2 / 11-2-7-5 / 11-2-7-6 | 대상 변경, 효과를 받지 않는 디지몬도 변경 가능, “~에 어택했다면” 선언 시 대상 기준 | 정상(`attackOnDigimon` 이벤트는 선언 시 대상) | 정상 |
| 11-5-1-1 / 11-5-1-2 / 11-5-1-3 | 성립: 시큐리티 ≥1이면 체크, 0이면 승리, 디지몬이면 배틀 | 정상 | 정상 |
| 11-6-1 / 11-6-2 | 어택 종료 시, 처리할 것이 남으면 종료하지 않음 | `endAttack`은 종료 시 유발만 큐잉(pending 해결 대기 없음) | 정상(경미) |
| 12-1-1 ~ 12-1-3, 12-1-5 | 블록은 ≪블로커≫ 액티브 디지몬이 1회에 1체, 현재 대상은 블록 불가 | 정상 (≪충돌≫ 강제도 처리) | 정상 |
| 12-1-6 | 어택 중 디지몬이 없으면 블록 불가 | 블록 UI가 표시됨 | 수정됨 (enterBlockCheck 가드) |
| 12-1-7 | 블록 절차: 선언 → 자신의 디지몬 1체 레스트 | 레스트 후 대상 변경(레스트 실패 시에도 진행하던 문제는 위 12-1-4로 수정) | 수정됨 |
| 13-1-1 ~ 13-1-4 | 1회 어택에 1회 체크(≪S어택≫으로 매수 증감), 1장씩, 강제 | 정상 | 정상 |
| 13-1-5 | 체크 중인 디지몬이 배틀 에어리어에 없으면 이후 체크 불가 | i>0에서만 검사 → i=0도 검사하도록 수정(위) | 수정됨 |
| 13-1-6 / 13-1-8-4 | 체크된 카드는 어디에도 속하지 않다가 남아 있으면 트래시 | 즉시 트래시에 넣어 두고, 효과로 이동하면 트래시에서 제거하는 방식(동등하게 동작) | 정상 |
| 13-1-7 / 13-1-8-1 | 체크한 디지몬 카드는 시큐리티 디지몬, 맨 위 공개(앞면은 그대로) | 정상 | 정상 |
| 13-1-8-5 | 체크 가능하면 다시 체크(승리 시 계속, 패배/동점 시 종료) | 정상 (≪재밍≫ 처리 포함) | 정상 |
| 14-1 / 14-2-1 | DP 비교: 높은 쪽 승, 낮은 쪽 패, 같으면 양쪽 패 | 정상 | 정상 |
| 14-2-2 / 14-2-3 | 패배 디지몬 즉시 소멸(동시), 시큐리티 디지몬은 패배해도 소멸하지 않음 | 정상 (순차 처리지만 pending은 나중에 해결) | 정상 |
| 14-2-4 / 14-2-5 | 배틀로 유발한 효과 해결, 배틀 종료 시 효과 | 디지몬 vs 디지몬의 “배틀 종료 시” 이벤트는 없음(해당 텍스트는 【시큐리티】 전용 52개뿐, 위에서 수정) | 정상(수정 반영) |
