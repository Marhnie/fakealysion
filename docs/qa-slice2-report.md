# 공식 카드별 Q&A 검증 리포트 (slice2, Q1751~Q2874, 1113건)

원문(일본어 Q&A)은 저장소에 복사하지 않았다 (`data/rulings`는 gitignore). 아래는 Q id 참조와 자체 요약만 담는다.

## 요약 수치

| 항목 | 값 |
|---|---|
| 전체 Q&A | 1113 |
| TESTABLE(T) | 1005 |
| RULE-ONLY(R) | 108 |
| NOT-APPLICABLE(N) | 0 (전 카드가 한국판 DB에 존재, 텍스트 불일치 미발견) |
| 시나리오 작성(개별 손작성) | 57개 (Q id 약 45개 커버) |
| 템플릿 시나리오 | 149개 카드 x "오픈 N장→기준별 1장씩 패에 추가(필수 추가)" 계열, 대응 Q 약 25건 |
| 빌드된 Q id 합계 | 70 (T 1005건 중 약 7%) |
| 통과 | 개별 57/57, 템플릿 149/149 (수정 후) |
| FAIL -> 엔진 수정 | 6건 (아래) |
| FAIL -> 미해결 | 0 |
| FAIL -> 해석/시나리오 오류(엔진 정상) | 약 8건 (색 조건 옵션, 2색 옵션 색 충족, 효과 있는 진화원 카드 선택 오류, API 인자 등) — 시나리오를 고쳐 통과 |

## 분류 방법 (정직한 공개)

- Q1751~Q2470 (약 297건)은 카드 한국어 효과 텍스트를 보면서 사람이 직접 분류했다.
- 나머지 816건은 **키워드 휴리스틱**(정의/의미를 묻는 질문 = R, 그 외 = T)으로만 분류했다. 개별 검토하지 않았으므로 T/R 경계는 부정확할 수 있다.
- `docs/qa-slice2-classification.json` = {qid: 'T'|'R'|'N'}.
- 개별 시나리오는 T 중 효과 해결/타이밍/비용/색·진화원 판정 위주로 우선 작성했다. T의 대다수(약 930건)는 **시나리오를 만들지 못했다** (검증되지 않음).

## 수정한 엔진 버그

1. **Q1759/Q1760 (BT8-074 / BT8-079 진화원 효과)**: "덱 위 N장 오픈 후 나머지 파기"가 `deckDiscard` 이벤트를 발생시켜 「덱이 파기되었을 때」 감시 효과(메모리+1)가 잘못 유발됨. 원인: `state.js resolveReveal`, `effects.js runRevealPick`가 나머지 파기 시 일반 덱 파기와 같은 이벤트를 발행. 수정: 두 지점에 `viaOpen:true`를 싣고 감시 조건(`state.js` 「덱이 파기되었을 때」 파서)에서 제외. (「이 카드가 덱에서 파기되었을 때」 옵션 카드 계열은 그대로 유지.)
2. **Q1782 (BT8-102 사마디·샨티)**: 이미 레스트인 상대 카드를 골라도 "이 효과로 레스트한 카드는 액티브가 되지 않는다"가 걸림. 수정(`shard1.js`): 효과 시점에 액티브였던 카드에만 다음 액티브 스킵 부여.
3. **Q1803 (BT9-012 그레이몬X 진화원 효과, P-072 동일 구현)**: "Lv.이 같은 카드 2장"을 **최상단 카드와 같은 Lv.**로 구현했음(정답은 진화원 카드끼리 같은 Lv.). 수정(`shard1.js survive`에 `comboOk` 추가, `graySurvive`).
4. **Q1862/Q1863 (BT9-071 드라큐몬)**: 「…카드 1장을 패에 추가하고, 1장을 파기한다」의 두 번째 "1장"이 필터 없이 아무 카드나 허용. 수정(`effects.js` revealPick 컴파일러): 개수만 적은 다음 스텝은 첫 스텝의 카드 조건을 이어받음. 같은 형식인 EX3-055, BT15-096, BT24-066도 함께 정정됨(정답 미확인 — 같은 문형이라 동일 적용, 개별 공식 Q&A 미확인).
5. **Q1873 (BT9-080 라구엘몬)**: 시큐리티 1장 이하일 때 "대신 …등장시킬 수 있다"가 강제되어 기본 효과(퍼플/옐로 DP6000 이하)를 못 쓰던 문제. `src/cards/shard62.js` 신규(등록: `index.js`): 천사형 후보가 있을 때만 확인하고, 아니면 기본 효과 실행.
6. **Q1897 (BT9-094)**: `destroySum`(DP 합계 상한 소멸)에 「DP 소멸 효과의 상한+1000」이 적용되지 않음. 수정(`effects.js`): `stat==='dp'`일 때 `S.dpDestroyCapBoost` 가산.

## 시나리오 파일

- `scripts/qa/lib2.mjs` (공용 하니스), `qa-slice2-a.mjs`(BT8/BT9 초반), `qa-slice2-b.mjs`(BT9 진화원/진화 시), `qa-slice2-c.mjs`(BT9 테이머/옵션/BT10), `qa-slice2-tpl.mjs`(공개→패 추가 템플릿).
- `_s2_cls.mjs`, `_s2_dump2.mjs`, `_s2_final.mjs`는 분류/집계 보조 스크립트.

## 검증되지 않은 것 (정직한 한계)

- **Q2471 이후 약 640건은 텍스트를 읽지 않았다.** 휴리스틱 분류만 됨.
- 읽은 범위(~Q2470)에서도 시나리오화하지 못한 T가 다수: 어택 흐름의 블록/카운터/리다이렉트 타이밍 (UI 코드 `main.js`에 있어 하니스가 일부(블록·attackTarget)만 흉내냄), 동시 유발 순서(Q1976 계열), 공개 정보/선택 주체(누가 고르는가) 계열, 디지크로스/조그레스 비용 계산, 진화 조건 무시 불가 계열(Q1789~1791, 1864 등), 토큰/육성 에어리어 이동 계열.
- 템플릿 테스트는 엔진 컴파일러 일관성(필수 추가) 확인이며 카드별 공식 문답을 일대일로 재현한 것은 아님. UI가 `min`을 강제한다는 전제.
- BT9-024(Q1825/1826)는 일반 파서(`state.js`)가 이미 "서로 같은 Lv."로 구현되어 있다고 코드 주석으로만 확인했고 시나리오는 없음.
- 회귀: `check-syntax` OK, `audit-effects` 100.0%, `soak 40` distinct errors 0, `test-cpu` OK. `test-all` 전체는 돌리지 않음.

---

## Round 2 (2회차 검증)

원문(일본어 Q&A)은 이번에도 저장소에 복사하지 않았고, Q id 참조와 자체 요약만 적었다.

### 수치

| 항목 | 값 |
|---|---|
| 대상 (1회차 이후 시나리오 없는 T) | 956건 |
| 2회차 시나리오 | 289개 (`scripts/qa/qa-slice2-r2-*.mjs`, 18개 파일) |
| 통과한 서로 다른 Q id | 253 (스모크 수준이라 뺀 7건 제외 후), 1회차와 겹치지 않는 신규 250 |
| 전체 T 1005건 중 누적 검증 | 약 320건 (1회차 70 + 신규 250, 일부 중복 제외) |
| FAIL -> 엔진 수정 | 6건 (아래) |
| FAIL -> 시나리오 오류 (엔진 정상) | 약 10건 (예: BT17-008 메모리 원인 착오, BT14-017 메모리 방향, 안개의 시큐리티 카드 자체 등장 등) -> 시나리오 수정 후 통과 |
| 회귀 | check-syntax OK, audit-effects 100.0%, soak 40 distinct errors 0, test-cpu OK, 1회차 a/b/c/tpl 전부 통과, slice1 st2/r2-c 통과 |

### 파일

- `lib-r2.mjs` (공용 헬퍼: byName/findEff/lvl), `qa-slice2-r2-xros.mjs` (디지크로스/조그레스/버스트), `-xa.mjs` (진화원 「X항체」 이름 조건), `-b/c/d/e/f/g/h/i/j/k/l/m/n.mjs` (카드별 묶음), `-family.mjs` (오픈 후 조건별 1장 패 추가 계열 41건), `-tamerevo.mjs` (테이머를 디지몬으로 취급해 진화하는 8장 x 6문답 = 40건), `-raising.mjs` (육성 에어리어 등장 4장 x 4문답 = 16건).
- 보조: `_s2r2_show.mjs`(카드 텍스트 출력), `_s2r2_count.mjs`(집계), `_s2r2_ids.json`(통과 Q id 목록).
- 같은 공식 문구가 여러 카드에 반복되는 계열은 데이터 주도 템플릿 하나로 묶었다 (family / tamerevo / raising / xa / DP상한 / 시큐리티 합계 / 시큐리티 3장 양쪽 처리).

### 수정한 엔진 버그

1. **Q2012/2017/2126 (BT10-087, BT10-088, BT11-095)**: 「자신의 테이머 아래의 카드도 디지크로스로 놓을 수 있다」가 능력을 가진 테이머 자신의 아래 카드만 후보로 삼았음. 공식은 다른 테이머 아래의 카드도 가능. 수정: `state.js planDigiXros`의 테이머 재료 루프가 자신의 모든 테이머 스택을 순회 (능력 보유 테이머 먼저). (Q2013/2018/2127 디지몬 진화원은 후보 아님은 기존대로 통과)
2. **Q1855 (BT9-067 라이덴몬)**: 「진화원 Lv.6 카드의 색 합계가 3색 이상이면 DP+3000 / 4색 이상이면 퇴화 1」의 조건이 범용 컴파일러에서 통째로 사라져 무조건 발동. 수정: 신규 `src/cards/shard90.js`에 색 합계 계산 스크립트 (index.js 등록).
3. **Q2046 (BT11-005 등, 무승부 배틀)**: 무승부로 공격측/방어측이 동시에 소멸할 때 공격측을 먼저 지워 그 시점에 아직 살아 있는 방어측 진화원의 「상대 디지몬이 소멸했을 때」 유발 효과가 발동했음 (공격측이 보유자인 경우만 기존에 통과). 수정: `resolveDigimonBattle` 무승부 처리 동안 두 스택에 `_simulDel` 표식을 달고, 소멸 이벤트 감시자 두 경로(`emitGameEvent` 일반 파서/HOOKS)에서 다른 쪽의 소멸에 대한 반응을 건너뜀.
4. **Q2073/2075/2078/2307/2309 (BT11-040/041/043, BT13-065/069 진화원 효과)**: 「명칭에 스카몬을 포함하는 다른 디지몬 1마리를 소멸시키는 것으로, 소멸하지 않는다」의 희생 후보가 자신의 디지몬으로 한정됨. 공식은 상대의 스카몬도 가능. 수정: `state.js trySurviveBySacrifice` 후보를 양쪽 필드로 확장, 소멸은 소유자 기준. (Q2074/2076/2079/2308/2310 서로 끼어들기 1회 제한은 기존 replAttempt 키로 통과)
5. **Q2691 (BT16-091)**: 조그레스 진화한 디지몬에게 《S 어택+1》을 부여한 뒤 어택 여부를 물어, 어택을 거절해도 키워드가 남았음. 공식은 「S 어택+1을 얻고 어택」 또는 「둘 다 안 함」. 수정: `shard11.js`에서 어택 확인 후에만 부여.
6. **Q1885 (BT9-083, 다른 에이전트의 shard80 회귀)**: 진화원 없이 1장뿐인 이 카드가 턴 시작 효과로 스스로 파기되고 있었음 (shard80.js의 새 스크립트가 최상단 카드 이동을 스택 전체 이탈로 처리). 공식은 파기되지 않음. 수정: `shard80.js`에 진화원 없음 가드 한 줄.

### 통과한 주요 계열 (요약)

- 디지크로스/조그레스/버스트: 비용 감소 불가 중 디지크로스 선언 가능(1757), 재료 0장은 디지크로스 아님(2255), 조그레스 결과는 액티브(2185), 조그레스가 아닌 진화에서도 앞부분 효과 발동(2622/2651), 버스트 진화는 자신의 테이머만(2284/2305), 조그레스 옵션 3장의 "등장만"/"조그레스만" 선택(2689~2695), 테이머 조그레스 보너스는 「~하는 것으로」 비용 미지불 시 후속 없음(2676/2678/2682).
- 「~하는 것으로」 계열 후속 처리 취소(2444/2558/2564/2595), 적절한 대상이 있으면 소멸을 일부러 피할 수 없고 면역 대상을 고르는 것은 가능(2710/2711/2718/2719/2739/2740/2744/2745), 대상이 없어도 자기 몫은 처리(2422/2447/2483/2485/2504/2506).
- 시큐리티 3장 정확히 3일 때 양쪽 반쪽 모두 처리(BT16 계열 2618/2630/2635/2636/2639/2644/2646), 「서로의 시큐리티 합계」는 양쪽 합(2287~2290), 시큐리티에서 직접 파기된 경우에만 유발(2345/2354/2518/2583), 레스트 상태의 「상대 디지몬 효과를 받지 않는다」는 시큐리티 디지몬의 【시큐리티】도 차단(2526/2528/2537/2640), DP 소멸 상한 +N은 "이 디지몬의 DP 이하" 같은 비수치 상한에 적용 안 됨(1802/2145/2714/2722).
- 옵션 사용 시 유발 계열: 옵션 메인 효과 뒤에 처리, 사용하지 않은 발휘(시큐리티)는 미유발, 손에서 코스트가 1 이하로 깎인 경우 미유발(1954~1956, 2778~2780, 2782/2783, 2788~2790).
- 육성 에어리어 등장(BT15-027/050/062/077): 【등장 시】 미발휘, 「등장했을 때」 미유발, 같은 턴 이동 후 어택 불가, 등장 불가 효과 중에는 불가(16건).
- 테이머를 디지몬으로 취급해 진화(8장): 진화 시 유발, 진화 드로우, 등장한 턴 어택 불가, 진화원 취급, 시큐리티 텍스트 미취득/진화원 효과 취득(40건).
- 「이름이 정해진 X항체」 조건: 특징만 가진 카드는 불충족(1806/1807/1810/1815/1827/1829/1834/1836/1850/1852/1899).
- 오픈 후 조건별 1장씩 패에 추가 계열: 한쪽만 있어도 추가, 양쪽이면 전부 필수 추가(41건, 카드별 실제 스크립트로 두 변형 실행).

### 해결되지 않은 것 / 참고

- 효과로 어택시키는 옵션(BT9-095/100, BT11-104/107 등)의 후보 목록에는 그 턴에 등장한 디지몬이 포함될 수 있다 (`canAttackPlayer`에 등장 턴 검사가 없고 `declareAttack`이 나중에 거절). 공식 결과(어택 불가, Q1898/1903/2135/2497 등)는 `declareAttack` 수준에서 확인했으나, 후보 선택 UX는 미수정 (볼텍스 예외 때문에 `canAttackPlayer` 전역 변경은 위험).
- 스모크 수준이라 통과 집계에서 뺀 시나리오: 1858, 1864, 1875, 2105, 2474, 2816, 1837. 단언이 약한 통과(예: 2057은 생존만 확인, 1785 두 번째 단언, 2608 상한만 확인, 조그레스 "진화 불가 제한" 비교는 만든 restriction 객체 사용).

### 검증되지 않은 것 (정직한 한계)

- T 956건 중 약 706건은 여전히 시나리오 없음. 주로: 동시 유발 순서 선택(1976, 2268~2283 계열: 하니스가 유발 순서를 고르지 못함), 「2장 이상 동시 등장」 디지크로스 계열(2014/2015/2019/2020/2026/2128/2129: 엔진은 한 장씩만 등장시켜 모델 자체가 없음), 턴 종료 시 어택 계열(2378/2423/2491: `startAttack`이 UI 코드라 하니스에서 흉내 불가), 공개 정보/선택 주체 계열, 진화원 이름/색 재정의(스카몬 변경 2081~2084), 토큰/육성 이동의 세부, 오픈 후 "부분 등장"의 나머지 처리, BT14~BT17 후반 카드 다수.
- Q2471 이후 분류는 여전히 키워드 휴리스틱이며, 이번에도 전부 읽지는 못했다.
- family 계열에서 템플릿이 다루지 못한 15장(BT10-019/058/096/097, BT11-012, BT13-087, BT14-051/063/088, BT15-027/050/062/064/077/096)은 개별 스크립트라 미검증.
