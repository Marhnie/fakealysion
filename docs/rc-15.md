# 룰북 Ver.4.2 제15장 (효과의 룰) 대조 결과

대상: 15-1 ~ 15-16 (약 110개 하위 규칙). 코드 리딩 + Node 실행 검증(`scratchpad/rc15/*.mjs`).
상태: **수정됨** / **열림** / **정상**(확인만 함) / **UI 한계**.

| 규칙 번호 | 룰 내용 | 엔진 동작 | 상태 |
|---|---|---|---|
| 15-1-3 | 금지하는 효과가 가능하게 하는 효과보다 우선 | 각 카드 훅(noAttack/preventLeave/effectImmune 등)이 상시 효과보다 먼저 검사됨 | 정상 |
| 15-1-4 / 15-4-1-2 | 효과의 처리 중 카드가 새 카드가 되어도(소멸 등) 그 효과는 끝까지 해결 | runScript는 소스 스택 생존 여부와 무관하게 전 명령 실행 | 정상 |
| 15-1-9/15-1-10 | 디지타마/토큰이 실제로 놓이지 않아도 「놓였을 때」 조건은 충족, 「늘었을 때」는 불충족 | 토큰은 트래시 push만 제외(purgeTokens/isToken 필터). 「트래시에 놓였을 때」 조건 이벤트가 토큰에 대해 발생하는지는 전용 이벤트가 없어 미검증 | 열림 (검증 불가) |
| 15-4-2-3 / 15-4-4-2 | 발휘 대기 효과는 1개씩만 발휘 | main.js pendingRunner가 1개씩 직렬 처리 | 정상 |
| 15-4-2-4 | 발휘 전에 조건을 다시 만족하면 다시 유발 | 이벤트마다 별도 pending 생성 | 정상 |
| 15-4-3-5 | 동시유발: 턴 플레이어 먼저(순서 선택), 다음 비턴 플레이어 | autoRunMandatoryPending이 턴 플레이어 큐 우선 + pickPendingOrder | 정상 (기존 수정 반영) |
| 15-4-4-3 | 발휘 대기 중 카드가 새 카드가 되거나 영역을 벗어나면 발휘 불가 | runPendingScript가 topId 스냅샷 비교(소멸 시 제외). 단 emitGameEvent(watcher) 경로 pending은 topId가 없어 검사 안 됨 | 수정됨 (watcher pending에도 `topId`/`inherited` 기록) |
| 15-4-4-4 / 15-4-4-5 | 발휘 대기 중 그 효과를 잃거나 유발 조건을 잃으면 발휘 불가 | 미구현(스택이 같은 카드면 그대로 발휘) | 수정됨 (`runPendingScript`: 진화원 유래 효과의 진화원 카드가 스택을 떠나면 「효과를 잃음」, 【자신의 턴】/【상대의 턴】 유발 조건이 사라지면 발휘하지 않음. 본문 내 임의 조건은 스크립트가 처리 시점에 판정) |
| 15-4-5-2 / 15-4-5-3 | 파생 유발(동시유발 해결 중 새로 유발)은 기존 대기 효과보다 먼저, 턴 플레이어 대기 중에도 비턴 파생 효과가 먼저 | 기존: 모든 대기 효과를 한 풀에서 선택(파생 우선 없음) → pending에 depth를 부여, 가장 깊은 depth 우선 풀로 제한 | 수정됨 (main.js autoRunMandatoryPending) |
| 15-5-2 | 하나의 유발 조건이 동시에 여러 번 발생해도 유발은 1회 | 시큐리티를 N장 동시에 파기/패로 이동 시 카드마다 `securityDecrease` 이벤트를 따로 emit → 【시큐리티가 줄어들었을 때】 효과가 N회 유발(턴 1회 제한 없는 카드) | 수정됨 (`emitGameEvent`가 하나의 효과 명령(`runOne`) 안에서 같은 플레이어의 효과로 인한 `securityDecrease`를 1회로 합침. 명령을 넘나드는 별개 손실은 각각 유발) |
| 15-6-1~3 | 처리 조건(~라면/~동안) 미충족 시 그 처리 불가, 다른 처리는 영향 없음 | compileToScript condition op이 해당 절만 게이트 | 정상 |
| 15-7-2 / 15-7-3 | 임의 처리 조건(~것으로)은 내용을 전부 실행해야 하며 일부만 실행 불가, 미실행 시 이후 처리 불가 | costGroup이 비용 선택 후 실제 지불량을 검증하지 않아, 패 파기 비용에서 0장을 골라도(또는 지불 실패해도) 이후 효과가 실행됨. 시큐리티 비용 N장은 canPay가 1장만 요구 | 수정됨 (effects.js costGroup: 지불 전후 패/시큐리티/레스트 검증, removeSecurity 필요 장수 합산) |
| 15-7-4 / 15-7-5 | 실행 불가여도 실행 선택 가능 / 이후 처리 불가여도 선택 가능 | 비용 불가 시 자동 건너뜀(선택 없이). 결과는 동일하나 「선택해서 비용 일부를 못 내는」 시나리오는 UI에서 선택 불가 | 정상(결과 동일) |
| 15-8-2-4 | 복수 상시 효과는 중복 | DP 합산 등 누적 | 정상 |
| 15-8-2-5 | 금지 효과 제외, 모순되는 상시 효과는 나중에 발휘한 것이 우선 | 「나중에」 타임스탬프를 관리하지 않음(효과별 개별 계산, 정의된 순서로 덮어씀) | 열림 |
| 15-8-3-2 | 유발형 효과는 룰/효과 처리 중 발휘 불가 | 유발은 pending 큐에 쌓고 처리 종료 후 실행 | 정상 |
| 15-8-3-5 | 소멸 시 유발은 원래 영역의 맨 위 카드가 유발/대기 | queueTriggersForStack이 최상위 카드 + 진화원 상속 효과를 큐잉, delStack 기록 | 정상 |
| 15-8-3-6 | 턴 종료 시 유발은 시점에 유발, 이후 메모리 변동과 무관 | engine.endTurn에서 즉시 큐잉 | 정상 |
| 15-8-3-8 | 유발 시점의 상황(예: 등록된 디지몬의 Lv.)을 참조 | 일부 스크립트가 처리 시점의 상태를 참조(예: 레벨 참조 op이 현재 스택을 재조회) | 열림 (카드별 편차) |
| 15-8-3-9-4 | 영역을 떠나서 유발한 효과의 처리 조건은 유발 시점 상태 참조 | deletedInfo(마지막 상태) 스냅샷 제공 | 정상 |
| 15-8-4-2 | 기동형(【메인】)은 자신의 메인 페이즈, 해결 중 처리가 없을 때만 선언 | activatableMainAbilities가 phase/activePlayer 검사, main.js `blockIfBusy` | 정상 |
| 15-8-4-3 / 15-14-1 | 기동형 [턴에 N회] 소진 시 선언 불가 | 상속/훅 기동(descriptor.activate) 경로의 정규식이 `\s`,`\d` 이스케이프 누락(`s*`,`d+`)이라 횟수 제한이 버튼 노출에 반영되지 않음 | 수정됨 (state.js activatableMainAbilities) |
| 15-8-4-4-1 | 임의 처리 조건이 있는 기동형은 조건을 실행할 수 있어야 선언 가능, 선언 후 반드시 실행 | 버튼은 항상 노출되고 발동 후 확인창에서 취소 가능(《트레이닝》만 별도 검사) | 열림 (UI) |
| 15-8-5-1~5 | 즉시형(~할 때) 효과는 요인 직전에 끼어들고 즉시형끼리만 동시유발, 요인 해결까지 1회씩 | 소멸 방지/진화 코스트 등 전용 훅(trySurvive*/hookPreventLeave/evoCost 옵션)으로 개별 구현. 즉시형끼리만 동시유발·1회씩 규칙은 일반화되어 있지 않음 | 열림 (부분 구현) |
| 15-9-1 / 15-9-2 | 강제 처리는 반드시 실행, 임의 처리는 선택 | isOptionalAutoEffect가 「~할 수 있다」만 확인창 | 정상 |
| 15-10-2-1~3 | 「X체」는 가능한 한 선택, 「X체까지」는 X 이하, 같은 대상 중복 선택 불가 | pickStack 반복 op에서 선택 uid 제외 처리(대부분). 일반 op의 중복 방지는 소스별 편차 | 정상(표본 확인) |
| 15-11-1-2 | 단체 처리는 대상이 새 카드가 되면 대상에서 탈락 | 대상 uid만 저장하는 op은 진화 후에도 계속 영향(즉시 해결되는 op이 대부분이라 영향 미미) | 열림 (지속형 부여만 해당) |
| 15-11-1-3-2 | 조건부 단체 처리로 부여된 효과는 이후 조건을 잃어도 유지 | 부여는 스택에 저장된 키워드/DP로 유지 | 정상 |
| 15-11-2-2 | 지속하는 전체 처리는 나중에 늘어난 대상에도 적용 | 「턴 종료까지 상대 디지몬 전체 DP-」 등은 등장 시점 스택에 적용됨 → 이후 등장 디지몬에는 미적용 | 수정됨 (`modifyDPAll`이 `addDpAllMod`로 「나중에 등장한 디지몬」에도 `effectiveDP`에서 동적으로 적용, 기간 만료 지원) |
| 15-12-1-3 / 15-12-1-5 | 정보 추가 효과는 코스트·Lv.·DP를 1개씩만, 나중 것이 덮어쓰기 | shard2 s2_overrideBase 등 카드별 구현, 일반 규칙 없음 | 열림 (카드별) |
| 15-13-2 | 부여된 효과는 위에 카드가 겹쳐져도 상태를 계승 | keywords/tempDP가 스택 필드에 저장되어 진화 후에도 유지 | 정상 |
| 15-14-1-1 / 15-14-1-4 | [턴 X회]는 발휘를 선택할 때마다 소비, 처리 불가여도 소비 | 발휘 전 소진 검사 후 소비. 임의 효과에서 「발동하지 않음」을 골라도 사전에 소비되던 문제 | 수정됨 (main.js runPendingScript: 확인창 통과 후에 소비) |
| 15-14-1-2 | [턴 X회] 소진 시 이후 그 턴 유발하지 않음 | watcher 경로는 유발 시점에 소진 검사/소비, pending 경로는 발휘 시 검사 | 정상 |
| 15-14-1-3 | 카드 1장마다 각각 횟수 소비 | 스택별·카드ID+태그별 키 | 정상 |
| 15-14-1-5-1 | 턴이 바뀌면 소비 횟수 리셋 | resetTurnEffectUses(턴 시작) | 정상 |
| 15-14-1-5-2 | 그 효과를 가진 카드가 새 카드가 되면 소비 횟수 리셋 | 진화/아츠/퇴화로 최상위 카드가 바뀌어도 turnEffectUses를 유지(진화원 상속 효과가 같은 키를 공유할 수 있음) | 수정됨 (state.js digivolve/artsDigivolve/retreat에서 초기화) |
| 15-14-2~5 | {패}/{트래시}/{육성}/{시큐리티} 아이콘 효과는 해당 영역에서만 유발·발휘 | zoneMarker 처리(육성은 queueTriggersFor, 패/트래시는 zoneMainAbilities, 시큐리티는 zoneMarker==='시큐리티') | 정상 |
| 15-15-1-1 | 어택 종료 효과는 처리 즉시 어택 종료 시점 | ctx.endAttack | 정상 |
| 15-15-2-2 / 15-15-5-1~3 | 효과를 받지 않는 카드는 효과 영향 없음(선택은 가능), 효과를 받게 되면 그 시점부터 영향 | effectBlocked로 destroy/bounce/rest/dpDown/retreat/srcTrash/other 차단, pickStack은 선택 가능하되 무효 | 정상 |
| 15-15-5-4 | 효과를 받지 않는 디지몬에 효과를 부여해도 그 효과를 가진 것으로 취급하지 않음 | grantKeyword가 면역을 검사하지 않아 《S 어택-1》 등이 그대로 부여됨 | 수정됨 (state.js grantKeyword: effectBlocked(…,'other') 검사) |
| 15-15-3-1~3 | 공개(오픈) 중에는 원래 영역의 카드 수가 증감하지 않은 것으로 취급, 오픈 시 「시큐리티가 줄었을 때」 등 미유발 | revealTop이 덱을 그대로 두고 slice, resolveReveal에서 이동 → 오픈 중 이벤트 없음 | 정상 |
| 15-15-3-6 | 공개한 카드를 덱 밑에 되돌리는 순서는 발휘한 플레이어가 선택 | resolveReveal이 공개된 순서 그대로 덱 밑에 넣음(순서 선택 UI 없음) | 수정됨 — resolveRevealOrdered가 orderCards로 플레이어가 순서 선택 (effects revealTop, shard7 revealPick) |
| 15-15-3-5 | 덱 공개 중 드로우는 공개하지 않은 카드에서 | 공개 처리(revealTop)와 즉시 함께 완결되므로 충돌 없음 | 정상 |
| 15-15-4-3 | 수치가 없는 효과는 수치 가감 불가 | dpDestroyCapBoost 등 명시적 상한 수치가 있는 경우만 가산 | 정상 |
| 15-15-6-2 | 디지크로스 대체 효과는 「같지 않은」 조건 슬롯 대체 불가 | 대체 효과(hookJogress/DigiXros alias) 자체 미모델링 | 열림 (미구현) |
| 15-15-7-2 / 15-15-7-4 | 다른 효과를 복수 발휘할 때 순서는 플레이어가 선택, 강제 효과면 선택한 효과의 임의 조건도 강제 | 「효과 1개 발휘」계열은 카드별 op에서 순차 실행(순서 선택 UI 없음) | 열림 |
| 15-16-2~9 | 【등장 시】【진화 시】【소멸 시】【어택 시】【메인】【자신/상대/서로의 턴】 유발 타이밍 | TRIGGER_TAGS 서브스트링 매핑, 이벤트별 queueTriggersForStack | 정상 (표본 검증: soak 30국 오류 0) |
| 15-16-10-1 / 15-16-10-2 | 【시큐리티】 효과는 발휘 대기 없이 즉시 발휘, 동시유발 시에도 최우선 | 시큐리티 효과가 일반 pending으로 쌓여 턴 플레이어의 다른 대기 효과(예: 시큐리티 감소 watcher)가 먼저 발휘될 수 있었음 | 수정됨 (main.js autoRunMandatoryPending: evt.kind==='security' 최우선 풀) |
| 15-16-11 | 턴 시작 시 효과는 액티브 페이즈 진행 시점에 유발, 액티브 전에 발휘 | engine.beginGame/endTurn에서 turnStart 큐잉 후 pending이 비워질 때까지 autoAdvance 정지 | 정상 |
| 15-16-12 | 턴 종료 시 유발 | engine.endTurn에서 turnEnd 큐잉 | 정상 |
| 15-16-13 | 메인 페이즈 개시 시 유발 | engine.nextPhase(breeding→main)에서 큐잉 | 정상 |
| 15-16-14 / 15-16-15 / 15-16-16 | 【카운터】【어택 종료 시】【이동 시】 | findCounterOptions / attackEnd 이벤트 / move 이벤트 | 정상 |

## 요약
- 확인한 규칙: 약 110개 (15-1 ~ 15-16 전체)
- 모순 발견: 13건 (수정됨 6 / 열림 7 이상 — 표의 상태 열 참조)
- 수정한 내용: 15-7-2/3 비용 전부 지불 검증, 15-4-5 파생 유발 우선, 15-16-10-2 시큐리티 효과 우선, 15-14-1 임의 효과 거절 시 [턴 X회] 미소비, 15-14-1-5-2 새 카드 시 횟수 리셋, 15-15-5-4 면역 대상 키워드 부여 차단, activatableMainAbilities 정규식 이스케이프 누락 버그(횟수 제한 미반영)
