# 룰링 감사 보고서 — 그룹 7 (g7)

담당 파일: `data/rulings/503039.json`(EX-12), `503040.json`(BT-26), `503041.json`(EX-13), `503101.json`(ST-1), `503102.json`(ST-2), `503103.json`(ST-3)

## 1. 요약 표

| 항목 | 수치 |
|---|---|
| 담당 파일 수 | 6 |
| 총 Q&A 엔트리 수 | 747 (190+249+262+9+20+17) |
| 커버하는 고유 카드 수 | 235 (EX-12 59, BT-26 78, EX-13 70, ST-1 9, ST-2 8, ST-3 11) |
| 고유 질문 텍스트 수 (중복 제거) | 371 — 같은 일반 규칙(예: "기재가 있는 카드", "관통 체크는 1회만", "카운터는 1회만" 등)이 여러 카드에 동일하게 반복 첨부됨 |
| 코드까지 추적/검증한 항목 | 약 25개 대표 규칙(빈도 상위 + 리스크 키워드 기반 선별) + 카드별 정밀 확인 6장 |
| 스캔만 하고 넘어간 항목 | 나머지 저빈도(n=1) 항목 대부분 — "예/아니오, 인쇄된 대로" 수준의 사소한 확인 |
| 발견·수정한 버그 | 3건 (아래 2절) |
| 의심되나 미해결(설계상 큰 수정 필요) | 1건 (아래 3절) |
| 시간 부족으로 건너뜀 | 아래 4절 |

## 2. 발견하고 수정한 버그

### 2-1. `S.cardMentions` (src/state.js) — "「X」가 기술되어 있는" 판정에 특징(types) 배열 누락

- **증상**: 공식 일반 Q&A(제 룰링 파일 중 66개 카드에 동일하게 첨부된 "기재가 있는 카드" 질문)에 따르면 "「X」가 기술되어 있는 카드"는 **명칭·특징·효과·진화원효과·〈룰〉·진화조건·조그레스·데지크로스조건·버스트진화·업합체·링크·어셈블리조건**을 모두 스캔해야 함. 그런데 기존 `cardMentions`는 카드명과 `effectKo`/`inheritedKo` 텍스트에 `「X」`가 **문자 그대로 인쇄**되어 있는지만 확인했고, 카드의 `types`(특징) 배열 자체는 전혀 확인하지 않았음.
- **재현**: `EX13-037` 듀나스몬의 진화 조건·어셈블리 조건이 둘 다 `「윗체르니」가 기술되어 있는 Lv.5`. `BT18-039` 미스티몬은 `types: ["마법전사형", "윗체르니"]`로 특징에만 "윗체르니"를 갖고 있고 카드 텍스트 어디에도 "윗체르니"라는 글자가 없음(이름에도 없음). 수정 전에는 `cardMentions('BT18-039', '윗체르니')`가 `false`를 반환해 이 카드가 EX13-037의 진화 재료/어셈블리 재료로 인정되지 않았음(같은 문제로 EX7-019, EX7-023, EX8-074, EX10-041 등 다른 세트 카드도 영향을 받음. `evoTargetPredicate`/`parseJogressSide`가 전부 `cardMentions`를 재사용하므로 조그레스·일반 진화·어셈블리·링크 조건에 동일하게 영향).
- **수정**: `cardMentions`에 `(c.types || []).includes(n)` 분기 추가.
- **회귀 테스트**: `scripts/qa/qa-audit-g7-mentions.mjs` (3개 시나리오, 통과) — types 전용 매치, 기존 매치 방식 유지, `evoTargetPredicate`를 통한 실제 진화 조건 매칭까지 확인.

### 2-2. `src/cards/shard8.js` `OPS.s8_playOrUse` / `OPS.s8_evolve` — "코스트를 지불하지 않고 등장/진화"가 코스트 락을 무시함

- **증상**: 공식 Q&A(제 룰링 파일 9개 카드에 첨부): `ST12-03`류 "지불하는 등장 코스트를 마이너스할 수 없다" 카드가 있을 때 "코스트를 지불하지 않고 등장시킬 수 있다" 효과를 발휘하면 — **효과 자체는 발휘되어 카드가 등장은 하지만, 코스트는 마이너스되지 않는다**(= 원래 코스트를 그대로 지불해야 함). 진화 버전(`BT5-021`류 "지불하는 진화 코스트를 마이너스할 수 없다")도 동일.
- 실제 코드에서는 `free: true`일 때 `S.isPlayCostLocked`/`S.isEvoCostLocked`를 아예 확인하지 않고 무조건 코스트 0으로 처리하고 있었음. `EX12-013/027/041/043/050`, `BT26-012`(이 6장은 이미 `delta: -2` 방식이라 원래도 정상이었음)과 달리, `BT26-054`/`BT26-049`/`BT26-077`/`EX13-045`(손패에서 등장하는 분기)는 `free: true`를 쓰고 있어 실제로 이 버그의 영향을 받음. 진화 쪽은 `BT26-099`가 `s8_evolve`에 `free: true`를 사용해 영향을 받음.
- **수정**: 두 OPS 모두 `wantsFree = i.free && !locked` 형태로 바꿔, 락이 걸려 있으면 원래 코스트(및 기존 델타/자체 할인 로직)를 그대로 지불하도록 변경. 락이 없을 때의 동작(완전 무료)은 그대로 유지.
- **회귀 테스트**: `scripts/qa/qa-audit-g7-costlock.mjs` (3개 시나리오, 통과) — `BT26-054`가 `ST12-03` 존재 시 카드는 등장하되 코스트 4를 그대로 지불하는지, 락이 없을 때는 여전히 무료인지, `s8_evolve`도 동일 패턴인지 각각 확인(`OPS.s8_evolve`는 `BT26-099`가 《딜레이》 래퍼를 함께 쓰고 있어 실제 사용 경로 대신 OPS를 직접 호출해 격리 테스트).

### 2-3. `src/effects.js` `parseConditionText` — "상대의 메모리가 N 이상/이하(이)라면" 패턴 누락 → EX13-060 알파몬의 조건절이 통째로 사라짐

- **증상**: `EX13-060` 알파몬 【진화 시】 "…DP -8000. **그 후, 상대의 메모리가 5 이상이라면, 메모리 +2.**"에서 뒤 문장의 조건이 전혀 인식되지 않음. 기존 파서는 `(자신의)?메모리가 N…` (자신 쪽, 상대측 명시 없음)과 `메모리가 상대 쪽의 N 이상…`(어순이 다름, "메모리가"가 먼저 옴)만 처리했고, 실제 카드 텍스트처럼 **"상대의"가 "메모리가"보다 앞에 오는 어순**은 매치되지 않았음. 이 경우 "…한다. 그 후, <조건>라면, <효과>" 컴파일 경로(`effects.js` 4049~4058행)가 `test`를 못 구하면 `if (!test) return prefix;`로 **뒤 절 전체를 조용히 버림** — 즉 이 버그는 "항상 발동"이 아니라 **"상대 메모리와 무관하게 절대 발동하지 않음"**이었음(메모리 +2가 완전히 죽어있던 효과).
- 같은 어순(`상대의 메모리가 N…`)을 쓰는 카드는 전체 DB에서 `BT25-019`, `BT26-078`, `EX13-060` 3장뿐. `BT25-019`는 이미 전용 스크립트(`shard7.js`)로 직접 `oppMemory()`를 써서 정상 동작 중이었고, `BT26-078`도 이미 전용 스크립트(`shard46.js`, `memOf(state, opp(...)) < 5`)로 정상 동작 중이었음. **`EX13-060`만 전용 스크립트가 없어 제네릭 컴파일러에 전적으로 의존**하고 있었고, 이 카드가 이번 버그의 실질적 피해자.
- **수정**: `parseConditionText`에 `^상대의\s*메모리가\s*(-?\d+)\s*(이하|이상)(?:이)?라면$` 분기 추가, `-mem(ctx)`(상대 부호) 기준으로 비교.
- **회귀 테스트**: `scripts/qa/qa-audit-g7-oppmemory.mjs` (3개 시나리오, 통과) — 실제 EX13-060 진화 세그먼트를 `drain()`으로 돌려 상대 메모리 5 이상/미만 두 경우 모두 확인, 컴파일된 조건 함수 자체도 단위 확인.

## 3. 후속 패스에서 수정 완료

### ≪관통≫(Piercing) 필수 시큐리티 체크가 "배틀할 수 있다" 스크립트 효과에서 아예 발동하지 않음 — **수정됨**

- 증상/원인은 최초 감사 그대로: 공식 Q&A(EX12-052 15개, EX13-045 13개, EX13-076 11개 등 다수)에 명시된 대로, 관통을 가진 자신의 디지몬이 카드 효과("이 디지몬과 상대의 디지몬 1마리로 배틀할 수 있다")로 상대를 배틀에서 소멸시키면 관통이 유발·발휘되어야 하는데, `S.resolveDigimonBattle`을 직접 호출하는 카드 스크립트 경로들이 반환된 `res.piercing`을 전혀 확인하지 않아 체크가 조용히 누락되고 있었다.
- **수정 설계**: 기존 `ctx.startAttack`/`ctx.attack`/`ctx.endAttack`과 같은 패턴으로 새 드라이버 능력 `ctx.securityCheck(attackerP, attackerUid, defenderP)`를 추가했다. 각 드라이버가 실제 구현을 갖는다 — `src/main.js`(대화형 UI: `scriptedSecurityCheck`, sel.pendingAttack과 별개의 `state._scriptedPierce` 오버레이로 렌더링), `src/cpusim.js`(헤드리스: 기존 `securityCheck(p,uid,op)` 클로저를 그대로 재사용), `scripts/qa/lib-s1.mjs`/`lib-s6.mjs`(실제 구현, 회귀 테스트용), 그 외 `scripts/qa/lib2.mjs`/`lib5.mjs`/`s3lib.mjs`/`scripts/lib-driver.mjs`(no-op 스텁 — 기존 `startAttack(){}`류 스텁과 동일한 관례).
- **"어택당 1회" 추적**: 새 `S.consumePierceCheck(state, attackerP, attackerUid)`(state.js)가 게이트 역할을 한다. `state.attackCtx`가 이미 이 스택의 것이면 그 pa 객체에 `pierceUsed` 플래그를 심어 추적한다. 문제는 `EX13-045`처럼 스크립트 안에서 `ctx.startAttack()`을 먼저 호출하는 카드 — 이 호출은 지연(main.js는 `setTimeout`, cpusim.js는 `effAtkQ`)되므로, 바로 다음 줄에서 배틀이 실제로 일어나는 시점엔 `state.attackCtx`가 아직 비어 있다. 이 경우엔 대신 공격 스택 자신에 `_pierceHeld` 홀드를 짧게 걸어 두고, 그 스택이 실제로 어택을 시작하는 순간(main.js `attackFlow`/cpusim.js `attack`/qa `attack()` 헬퍼가 `pa`를 만들 때) 그 홀드를 `pa.pierceUsed`로 그대로 흡수하도록 했다. 결국 어택이 끝내 선언되지 않은 홀드가 남는 것을 막기 위해, 그 스택의 다음 액티브 페이즈(engine.js `nextPhase`의 unsuspend 루프)에서 스윕한다. 설계 근거와 트레이드오프는 `S.consumePierceCheck`의 코드 주석에 상세히 적어 두었다.
- **재진입(reentrancy) 함정과 그 수정**: `ctx.securityCheck`는 카드 스크립트가 실행되는 도중(즉 그 스크립트를 담은 pending 트리거가 아직 `resolved`되지 않은 상태) 호출되는데, 실제 시큐리티 체크는 체크된 카드의 【시큐리티】 효과를 마저 처리하기 위해 각 드라이버의 pending 드레인 루틴을 다시 불러야 한다. 이걸 그냥 재귀 호출하면 드레인 루프가 "아직 안 끝난" 바깥쪽 트리거를 다시 집어서 스크립트 전체를 통째로 두 번 실행해 버리는 버그가 실제로 재현됐다(`qa-ex13-c.mjs`의 EX13-045 조그레스 테스트가 `_qaAtk` 길이 1 대신 2를 반환하며 발견). 고쳐서: 트리거를 집어 실행을 시작하는 시점에 `t._running = true`로 표시해 두고, 모든 pending 선택 로직(`cpusim.js`/`lib-s1.mjs`/`lib-s6.mjs`의 `drain()`, `main.js`의 `autoRunMandatoryPending`)이 `_running`인 항목은 건너뛰도록 했다 — 중첩 호출은 방금 생긴 새 트리거(체크된 카드의 【시큐리티】 효과 등)만 집는다. main.js는 추가로 `autoRunMandatoryPending`의 단일 실행 가드(`pendingRunner`)가 재진입을 막아 새 트리거가 영영 안 돌아가는 문제가 있어, 그 가드를 우회하는 전용 `drainNestedPending()`을 새로 만들었다(간단화: 동시 유발이 여럿이면 플레이어에게 순서를 묻지 않고 첫 번째를 그냥 집는다 — 체크된 시큐리티 카드 한 장이 동시에 여러 트리거를 거는 경우는 실질적으로 없다고 보고 채택).
- **수정한 카드 스크립트 호출부**: `src/cards/shard8.js` `OPS.s8_battle`(→ `shard14.js`의 `n5_battle` 별칭도 함께 적용됨), `src/cards/shard42.js` `EX12-052`, `src/cards/shard130.js` `EX13-044`(서로의 턴 배틀)/`EX13-076`/`EX13-077`, `src/cards/shard7.js` `BT25-020` — 전부 `S.resolveDigimonBattle` 호출 직후 `res.piercing`이면 `ctx.securityCheck`를 호출하도록 통일.
- **회귀 테스트**: `scripts/qa/qa-pierce-scripted-battle.mjs`(3개 시나리오, 전부 통과) — (1) 어택 없이(스크립트만으로) 발휘한 배틀이 관통이면 체크가 정확히 1회 발동, (2) 관통이 유발되지 않으면 체크가 발동하지 않음, (3) 【어택 시】 스크립트 배틀(다른 상대 디지몬 대상)이 먼저 관통 체크를 1회 소비하면, 같은 어택의 자연스러운 종료 배틀이 또 다른 상대를 소멸시켜도 2번째 체크는 발동하지 않음(16-7-3).

## 4. 시간 부족으로 건너뛴 것 (정직하게 밝힘)

- 371개의 고유 질문 텍스트는 전부 읽고 훑었지만, **코드까지 직접 추적/검증**한 것은 빈도(반복 첨부 횟수)와 리스크 키워드(동시유발, 타이밍, 진화원, 소멸/파기, 시큐리티, 코스트 등) 기준 상위 약 25개 일반 규칙 + 개별 카드 6장(EX13-045/EX12-052/EX13-076/EX12-047/EX13-060/EX13-064) 정도로 제한함. 나머지 n=1(카드 1장에만 첨부된) 저빈도 질문들, 특히 "예/아니오, 인쇄된 그대로입니다" 유형은 리스크가 낮다고 판단해 코드 대조 없이 넘어감.
- 구체적으로 검증하지 못한 항목:
  - `{트래시}`/`{시큐리티}` 스코프 한정 효과("그 카드가 트래시/표시 시큐리티에 있는 동안만 발휘 가능")의 일반 규칙 — 관련 카드(`BT26-078`/`BT26-079`/`BT26-082`/`BT26-100` 등) 각각의 전용 스크립트가 있는 것은 확인했지만, 일반 규칙 자체가 엔진에 올바르게 강제되는지는 개별 확인 못함.
  - "덱이 증가했을 때" 트리거(`BT26-001`/`BT26-015`/`BT26-060`, 덱이 줄었다 늘었다 해도 "증가" 판정이 되는지 등의 세부 사항) — 코드 추적 안 함.
  - "DP 소멸 효과의 상한 +N000"(`EX13-007`/`EX13-010`, 숫자가 적힌 소멸효과만 적용되고 "이 디지몬의 DP 이하" 같은 비고정 소멸효과엔 적용 안 됨) — 코드 추적 안 함.
  - `EX13-031` 스카몬 대왕의 "원래 명칭·색·DP로 변경" 이후 〈룰〉로 파생된 정보/효과로 얻은 정보/진화·퇴화 후 정보 유지 등 다층 규칙(엔트리 6개) — 로직이 복잡해 이번엔 코드 대조를 생략.
  - `EX12-034`/`EX12-064` 등에서 "동시에 여러 【서로의 턴】 효과가 있을 때 룰체크 끼어들기 순서"류의 매우 세밀한 순서 규정 — 기존 slice1~6 감사에서 이미 폭넓게 검증된 동시유발 처리 인프라를 신뢰하고 개별 재확인은 생략.
- 이미 확인해서 "정상"으로 판단한 것들(버그 아님, 참고용):
  - 카운터 효과 1회/어택 제한, 관통 시큐리티 체크 1회 제한, DP 0 즉시 소멸 안 함(룰체크 배치), "Lv.N 이상의, A 이거나 B" 양쪽 모두에 Lv 조건 적용, 시큐리티 체크 시 【시큐리티】 효과 우선 발휘 후 나머지는 턴 플레이어측부터, "-N:" 줄이 본문에 안 붙음(EX13용 기존 테스트로 확인), 테이머로부터 진화(`EX13-064` ← `EX13-074` 키시베 리에, `canEvolveAny`가 동적으로 올바르게 인식함을 실측 확인) 등은 모두 코드/실측으로 이미 정상 동작 확인함.

## 5. 회귀 테스트

- `node scripts/check-syntax.mjs` — 154 files OK
- `node scripts/audit-effects.mjs` — 100.0% coverage 유지 (8168 segments)
- `node scripts/soak.mjs 40` — distinct errors 0
- `node scripts/test-cpu.mjs` — distinct errors 0, RESULT: OK
- 신규 회귀 스크립트 4개 모두 통과: `qa-audit-g7-mentions.mjs`(3/3), `qa-audit-g7-costlock.mjs`(3/3), `qa-audit-g7-oppmemory.mjs`(3/3), `qa-pierce-scripted-battle.mjs`(3/3, 관통 후속 패스)
- 기존 EX13 관련 스위트(`qa-ex13-a/b/c/d/e`, 총 110개 시나리오) 전부 통과 — 이번 수정이 기존 커버리지를 깨지 않음을 확인.
- 관통 후속 패스: `scripts/qa/*.mjs` 207개 파일 전체 재실행 — 실패 1건(`qa-slice2-r2-m.mjs` Q1784, BT8-105/`shard1.js` 관련, 이번 변경과 무관한 기존 결함, `docs/audit-rulings-g8.md`에 이미 별도 기록됨)을 제외하고 전부 통과. 특히 `qa-slice6-d.mjs`(EX12-052 G186/G188), `qa-slice6-r2-f.mjs`, `qa-chain-attack-only.mjs`, `qa-retreat-rules.mjs`, `qa-audit-g7-*` 등 공유 라이브러리(`lib-s1.mjs`/`lib-s6.mjs`)를 쓰는 스위트들도 전부 정상.
