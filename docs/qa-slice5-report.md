# 공식 카드별 Q&A 적합성 검증 — slice 5 보고서

대상: `data/rulings/slice5.json` (1113건, 447종 카드, 전부 우리 DB에 존재). 원문은 저장소에 복사하지 않았고, 시나리오에는 Q 번호(주석)와 기대 결과의 요약만 적었다.
시나리오: `scripts/qa/qa-slice5-*.mjs` (19개 파일, 공용 헬퍼 `scripts/qa/lib5.mjs`), 일괄 실행 `node scripts/qa/run-slice5.mjs < /dev/null`.
분류 결과: `docs/qa-slice5-classification.json` ({Q번호: 'T'|'R'|'N'}), 생성 스크립트 `scripts/qa/_cls5.mjs` + 예외 목록 `scripts/qa/_N5.json`.

## 숫자

| 항목 | 값 |
|---|---|
| 전체 Q&A | 1113 |
| TESTABLE (T, 원리상 재현 가능) | 947 |
| RULE-ONLY (R: 용어 정의·유발 순서 선택 등) | 159 |
| NOT-APPLICABLE (N: 수동 처리 카드 등) | 7 |
| 시나리오 작성(built) | 111개 (같은 패턴이 여러 카드·Q에 걸치므로 약 160건의 Q를 커버) |
| 통과(수정 후) | 111 / 111 |
| 최초 실패 → 수정 완료 | 시나리오 34개 실패 → 엔진 버그 10건(아래) |
| 실패 → 미해결 | 0 |
| 해석 불일치(내 시나리오·해석이 틀린 경우) | 4건 (BT23-050 픽스처 없음, 5724의 "효과를 받지 않음"이 자기 효과에는 안 듣는 것, anyside 후보 집계 버그, EX10-061 속공 대상에 본인 포함 착각) — 엔진은 정상이었고 시나리오를 고침 |

주의: T 분류는 "구체적 보드 상황과 결과가 답에 적혀 있음"이라는 뜻이지 "구현했다"는 뜻이 아니다. 실제로 시나리오를 만든 것은 T 947건 중 약 160건이며, 나머지 약 790건은 **검증하지 않았다**(아래 미검증 목록).

## 수정한 엔진 버그

1. **옵션 사용 코스트 조건 (Q5449–5518 계열, BT10-032 / BT17-031 / 032 / 038 / BT19-030 / 034 / EX2-003 / 021 / EX8-031 / EX5-021 등)**
   - 원인: `state.js`의 "사용 코스트 N 이상의 옵션 카드를 사용했을 때" 파서가 실제 지불 코스트(`useCost`)로 비교. 지불 코스트 경감이나 효과로 코스트 없이 사용한 경우 유발되지 않았다(공식: 유발함). `shard4.js`의 `optUsed2`는 반대로 패의 카드 자체 코스트 감소(BT2-099/BT8-097)를 무시해 유발해 버렸다(공식: 유발 안 함).
   - 수정: `useOptionCard`/카운터 사용 경로가 `baseCost`(카드 자신의 문구로 줄어든 코스트, 지불 경감 전)를 이벤트에 싣고, 파서와 `optUsed2`가 `baseCost ?? 인쇄 코스트`로 비교.
2. **BT23-024 포세이도몬 (Q5247–5252, 6025, 6026)**: 【서로의 턴】 "링크했을 때" 효과가 엔진에 없어 죽어 있었고(수동 목록에도 잡혀 있던 항목), 레스트 봉쇄 자체도 구현이 없었다. `shard65.js`에 훅+스크립트, `state.js`에 `restExceptTopLocked`(매 판정마다 "가장 등장 코스트가 높은 디지몬" 재평가, 토큰은 코스트 없음 취급)을 추가하고 `canRestByRule`/`restStack` 양쪽에 연결.
3. **P-203 / BT23-059 (Q5197, 5323 등)**: "배틀 에어리어의 옵션 카드 1장을 파기하는 것으로" 코스트가 no-op이라 옵션이 없어도 효과가 나갔고 옵션도 파기되지 않았다. `shard65.js`에서 자신/상대 어느 쪽 옵션이든 선택·파기하고, 없으면 나머지 효과를 진행하지 않도록 구현.
4. **BT23-014 (Q5227/5228)**: 트래시 등장 봉쇄가 "트래시 주인" 기준으로 판정되어, 자신의 드라고몬 효과로 상대 트래시에서 상대 디지몬을 등장시키는 것까지 막혔다. `playFreeFromZone`이 효과 발휘자(`_fxSrc.player`) 기준으로 판정하도록 수정.
5. **【소멸 시】 대기 효과 (Q5230, 5593, 5758, 5905, 5913 계열)**: 소멸한 카드가 다른 효과로 트래시를 떠난 뒤에도 【소멸 시】(진화원 효과 포함)가 그대로 발휘되었다. `pendingCardLeftZone`을 추가하고 `main.js`, `cpusim.js`(그리고 시나리오 헬퍼)의 발휘 직전 검사에 연결. 근사: 같은 카드 번호의 다른 사본이 트래시에 있으면 남아 있는 것으로 본다(카드 인스턴스는 추적하지 않음).
6. **BT23-083 (Q5356)**: 【서로의 턴】(앞면 시큐리티 추가 시) 미구현이었음. 레스트 코스트를 지불해야 메모리 +1과 드로우가 나오도록 `shard65.js`에 구현.
7. **EX10-061 아포카리몬 (Q5785/5786)**: 진화원에서 "명칭이 서로 다른 것 1장씩" 등장시키는 효과가 수동 처리였다. 이름별 1장씩 전부(부분 선택 불가), 속공 부여, 턴 종료 시 소멸까지 `shard65.js`에 구현.
8. **EX11-004 (Q5789)**: 뒷면 시큐리티가 앞면이 되는 것도 "앞면 시큐리티가 늘어남"인데 이벤트가 없었다. `secFlipTopFaceUp`이 `faceUpSecurityFlipped`를 내보내고 EX11-004가 이를 받도록 수정(EX11-003은 "놓여졌을 때"만이라 그대로 — 시나리오로 확인).
9. **EX11-011 / EX11-046 (Q5796, 5895)**: 토큰(등장 코스트 없음)이 "가장 등장 코스트가 높은 디지몬"으로 골라져 살아남았다. 토큰은 후보에서 제외, 코스트 있는 디지몬이 없으면 전부 소멸(`shard7.js`).
10. **BT24-037 (Q5618/5619)**: 이탈 시 진화원 등장(【서로의 턴】)이 수동 처리였다. 후보 = Lv.4 이하 & (TS 또는 옐로/레드)로 `shard65.js`에 구현(자신의 효과 이외로 이탈할 때만).

수정한 파일: `src/state.js`, `src/main.js`, `src/cpusim.js`, `src/cards/shard4.js`, `src/cards/shard7.js`, `src/cards/shard42.js`, 신규 `src/cards/shard65.js`(+ `src/cards/index.js` 등록). 다른 에이전트가 동시에 수정하는 파일이라 모두 읽은 직후 문자열 단위(정확 일치·1회) 치환으로만 고쳤다.

## 미해결

없음. (다만 아래 "검증하지 않은 것"은 미검증이지 통과가 아니다.)

## 통과한 것(엔진이 이미 공식 답과 일치)

【진화 시】 효과 봉쇄 계열(ST22-04, BT23-034: 유발 안 함, [턴 1회] 미소모, 어택 시 반쪽은 정상), 옵션 사용 이벤트 계열의 나머지 패턴, 육성 에어리어 등장 봉쇄(BT9-033/BT9-047/BT14-009), "디지몬 1마리를 레스트" 계열의 자신/상대 선택 후보, 후보 필터(P-207, BT23-012), 턴 종료 소멸(EX4-063, BT23-017), 시큐리티 0장 처리(BT23-031, ST23-03, BT25-036/037, BT24-101), 링크 불가 카드, 육성 에어리어 진화 코스트 -1 미적용, DP0 소멸 시점(BT23-050, BT23-035), 「으로써」 비용 미충족 시 이후 처리 불가(EX10-063 등 수동으로 확인 가능한 것만), 앞면 시큐리티 셔플·체크(BT23-015 계열), ST22-06, EX4-060 진화원 등장 등.

## 검증하지 않은 것 (정직한 목록)

- T 947건 중 약 790건은 시나리오를 만들지 않았다. 특히 다음은 손대지 못했다: 디지크로스/어셈블리/조그레스/아푸합체 계열(Q5175–5179, 5214, 5240, 5394 등), 어택 중 새 어택 선언 불가(Q5223, 5443, 5444, 6110: 시나리오 헬퍼가 `startAttack`을 흉내만 내므로 main.js 경로를 검증하지 못함), 진화 코스트 중첩(Q5193–5196의 P-202 −2 등)과 트레이닝 옵션 동시 진화(Q5203, 5204, 5210, 5211 등), 연계(≪연계≫)로 방금 등장한 디지몬을 레스트하는 순서(Q5222, 5235 등), 유발 순서·파생 유발(Q5257, 5294, 6027 등), 링크 상한/링크 카드 파기, 테이머 아래 카드 이동 순서(Q5174, 6170 등), 아츠 진화(Q6166, 6215, 6313 등), 「효과를 받지 않는다」 부여/해제 시점(Q5324–5329: 기존 `test-immune-grants.mjs`가 일부 다룸).
- R(159건)은 시나리오 없이 답의 내용을 규칙 참조로만 분류했다. "플레이어가 발휘 순서를 고른다"는 UI 선택이라 엔진 시나리오로 검증하지 않았다.
- N(7건): BT24-060(Q5782), EX11-005(Q5791/5792), EX11-038(Q5865), BT25-043(Q6313, 6167), BT15-102(Q6241)는 수동 처리 또는 미구현 효과라 검증 대상에서 뺐다.
- `pendingCardLeftZone`은 카드 인스턴스를 구별하지 못하는 근사다. 토큰의 "코스트 없음"은 이번에 고친 카드(BT23-024, EX11-011, EX11-046)에서만 반영했고, 다른 "가장 코스트가 낮은/높은" 카드(예: BT23-059의 최저 코스트 소멸)는 토큰을 코스트 0으로 취급한 채 남아 있다.
- 시나리오는 실제 엔진 함수를 쓰지만 `main.js`의 UI 발휘 루프는 헬퍼(`scripts/qa/lib5.mjs`의 drain)가 흉내 낸 것이다. 그래서 `main.js`/`cpusim.js`에 넣은 【소멸 시】 검사는 코드 리뷰와 회귀 스위트(soak/CPU)로만 확인했고 브라우저에서 직접 확인하지는 않았다.

## 사고 기록

작업 초반에 공용 이름 `scripts/qa/lib.mjs`가 이미 다른 slice(6번)의 헬퍼로 존재하는 것을 모르고 덮어썼다. 원본은 복구할 수 없어 `lib.mjs`는 `lib2.mjs`를 재수출하는 임시 껍데기(경고 주석 포함)로 두었고, slice 5 헬퍼는 `lib5.mjs`로 분리했다. `qa-slice6-*.mjs`, `_c.mjs`, `_cls.mjs`가 쓰던 `plain/plainTamer/plainOption/W/scenario/run` 내보내기는 복구되지 않았으니 slice 6 담당이 자기 헬퍼를 다시 넣어야 한다.

## 회귀 확인

`node scripts/check-syntax.mjs` OK(124 files), `audit-effects` 커버리지 100.0%, `soak 40` distinct errors 0, `test-cpu` OK, `fuzz 60 --seed 424242` 발견 0건, `test-all --quick` 21단계 전부 PASS(이후 소규모 수정 뒤 개별 재실행도 OK).


---

# Round 2 (2차 검증)

시나리오: `scripts/qa/qa-slice5-r2-*.mjs` (14개 파일, 공용 헬퍼 `lib5.mjs`), 일괄 실행 `node scripts/qa/run-slice5.mjs < /dev/null` (1차+2차 합계 301/301 통과).
커버 목록: `docs/qa-slice5-r2-covered.json` ({Q번호: 시나리오 파일}), 생성 스크립트 `scripts/qa/_s5r2manifest.mjs`. 원문은 복사하지 않았고 Q 번호와 기대 결과의 요약만 적었다.

## 숫자

| 항목 | 값 |
|---|---|
| 2차에서 새로 커버한 T Q 번호 | 307건 (목표 250 이상) |
| 그중 카드별로 직접 돌려 본 것 | 옵션 사용 계열 61, 보안 순서 22, 소멸 동시 7, 배틀 승리 18, 링크 옵션 14, 마인드 링크 6, 어플 합체 6, 조그레스 7, 테이머 아래 14, 「ことで」 비용 12 등 |
| 같은 답이 카드마다 반복되는 규칙(템플릿 수준)으로 커버한 것 | 앞면 시큐리티 48건(BT23-015 계열 14장의 동일 판정), 「효과를 받지 않는다」 42건, 테이머의 「디지몬으로도 취급」 42건. 엔진 경로가 카드와 무관한 규칙이라 카드 1장 기준 시나리오 1개로 검증했고, 같은 문장의 Q 번호를 한꺼번에 계상했다 |
| 남은 T(번호로 인용되지 않은 미검증) | 약 515건 (1차 종료 시점 번호 미인용 822건 중 307건 해결. 이 중 일부는 1차의 템플릿 시나리오가 실질적으로 같은 경로를 이미 확인했을 수 있으나 번호를 인용하지 않았으므로 미검증으로 계상) |
| 최초 실패 → 수정 | 엔진/데이터 버그 8건 (아래) |
| 해석 불일치(시나리오 쪽 오류) | 3건 (BT24-040이 「레스트할 수 없다」를 주므로 어택 불가는 정상, 하니스의 링크 카드 상속 효과 판정, 픽스처 부재) |

## 수정한 버그 (Q 번호, 카드, 근본 원인, 수정)

1. **Q5394 EX10-017 / EX10-019 / EX10-030 / EX10-073**: 한국어 카드 텍스트에 〔어플 합체〕 줄이 통째로 빠져 있어(영문 텍스트에는 있음) 어플 합체를 할 수 없었다. `state.js` 로드 시 정규화에서 영문 [App Fusion] 줄을 명칭으로 한국어 줄로 재구성(「A」 & 「B」 : 코스트 0).
2. **Q5256/5318 BT23-027, BT23-050 (베타몬 BT23-017 진화원 효과)**: 진화할 수 없는 디지몬이 자신의 【등장 시】 조그레스의 재료가 될 수 있었다. `fuseStacks`가 재료의 `evolveTargetRestriction`을 검사해 진화 불가 재료면 조그레스를 거부.
3. **Q6212 ST24-06 (및 같은 코스트를 쓰는 카드)**: 「테이머 아래의 뒷면 카드를 아래에서부터 2장 파기」가 한 명의 테이머에게 2장 이상 있어야만 지불 가능했다. 공식 판정은 여러 테이머에 1장씩(합계 2장)도 가능. `shard8.js`(COST.tamerUnder/payTamerUnder)와 `shard20.js`(trashFaceDown, ownTamer)를 합계 기준·카드 1장씩 테이머 선택으로 수정.
4. **Q5329 계열(8건, BT23-032 등)**: 「효과를 받지 않는」 디지몬에게 부여해 둔 발동형 효과(【어택 시】 등)가 발동 시점에 면역 여부를 보지 않고 발동했다. `shard2.js` grantEffect가 부여자(_fxSrc)를 기억하고 queueGranted가 발동 시점에 그 출처에 대한 면역이면 건너뛴다.
5. **Q5980 계열(9건, BT12-092 계열 「디지몬으로도 취급」 테이머)**: 유예/적용 기록(`deferred`/`applied`)에 부여자의 `alsoDigimon`이 저장되지 않아, 「디지몬의 효과를 받지 않는다」 상대 디지몬에게 걸린 테이머(디지몬 취급)의 효과가 정산 단계에서 다시 적용되었다. `state.js` grantGate의 기록 3곳에 alsoDigimon 보존.
6. **Q5986 계열 BT17-087 【등장 시】 / AD1-021 【자신의 턴 종료 시】**: 최건우를 디지몬으로 취급하지 않고 블로커·DP만 부여하려다(테이머는 DP가 없어 무효) 사실상 아무 효과가 없었다(AD1-021은 텍스트의 「으로 취급하고」 표기 때문에 컴파일 실패). 신규 `src/cards/shard75.js`로 두 카드를 s2AsDigimon 방식으로 구현(index.js 등록).
7. **Q5818 계열 EX11-026 / EX11-032**: 「배틀에서 이겼을 때/승리했을 때」 훅에 문구 정보(has)가 없어 시큐리티 디지몬과의 배틀 승리에 반응하지 않았다(`shard7.js`).
8. **하니스**: `lib5.mjs` drain이 링크 카드에서 온 상속 효과를 「효과를 잃음」으로 오판(실제 main.js는 링크 카드를 인정). 하니스만 main.js와 일치시켰고 엔진 결함은 아님.

주의: 작업 중 디버그 확인용으로 `state.js`를 백업본으로 되돌리는 명령을 2회 실행했다(다른 에이전트가 그 수 초 사이에 `state.js`를 수정했다면 그 변경이 지워졌을 수 있다). 이후 회귀(syntax/audit/soak/CPU/slice5 301건)는 통과했으며 누락 증상은 관찰하지 못했다.

## 미해결

없음(실패로 남긴 시나리오 0). 다만 아래는 미검증이다.

## 검증하지 않은 것 (정직한 목록)

- 시나리오를 만들지 않은 T 약 515건. 주요 미검증:
  - 디지크로스/어셈블리 계열: Q5176–5179(EX10-064), 5200/5397(P-205), 6060/6061/6082(AD1-006/013 진화원 등장 시 디지크로스 대상), 6247/6293(ディアナモン 진화원 효과 유발 여부), 5779/5780(밀레니엄몬 되돌리기 선택), 5940/6080(AD1-012 상대 턴 조그레스 후 진화 시 발휘 순서).
  - 연계·유발 순서: Q5257/5319(연계 2개+아택 시 동시 유발), 5235/5300/5316(연계로 방금 등장한 디지몬 레스트), 5444/5443(어택 중 새 어택 선언 불가·돌진 유실 — 하니스가 startAttack을 흉내내므로 main.js 경로는 미검증).
  - 옵션 사용 「타이밍」(Q5407 계열: 메인 효과 뒤에 발휘) — main.js UI 순서라 미검증. ST22-01, LM-023, P-223은 보유 조건이 특수해 이번 옵션 템플릿에 포함하지 않음.
  - 테이머 아래 뒷면 카드 계열의 「순서를 바꿀 수 없다」「내용 확인은 본인만」(Q6171/6172 계열 24건)은 UI 규칙이라 엔진 시나리오로 검증할 수 없음.
  - 「【진화 시】 효과는 발휘하지 않는다」 부여 계열의 「다른 효과로 발휘 불가」「ことで 조건만 처리 불가」(Q5260/5261 계열), 링크 옵션의 「육성 에어리어에도 링크 가능」(Q5685 계열), 앞면 시큐리티 관련 BT23-086·EX11-041 개별 문항(Q5867/5873–5875).
  - 카드별 「ことで」 비용 계열은 메인 페이즈 개시 시 4장(반납형)만 카드별로 확인했고 레스트형(BT23-078/079, BT24-085/087, EX11-054/056/062 등)과 옵션 【메인】형(P-204, BT23-025/030, BT24-099)은 미검증.
  - BT25-020(TS 픽스처 없음), 링크 옵션 ST22-08/09/11의 「링크 효과가 디지몬의 효과」 개별 확인(해당 링크 효과에 상대 대상이 없음), AD1-021·BT13-020·BT13-099·BT21-096의 개별 발휘 경로(규칙 자체는 템플릿으로 확인).
  - 템플릿 계상 항목은 엔진 경로가 같다는 전제이며, 카드별 개별 예외가 있으면 놓칠 수 있다.
- 시나리오의 「불가 코스트」는 하니스(drain)로 발휘하므로 `main.js`/`cpusim.js` 실제 UI 루프에서의 확인은 아니다.

## 회귀 확인

`check-syntax` OK(132 files), `audit-effects` 미커버 0, `soak 40` distinct errors 0, `test-cpu` OK, `run-slice5` 301/301(1차 시나리오 재실행 포함).
