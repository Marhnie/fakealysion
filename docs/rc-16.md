# 룰북 Ver.4.2 제16장 (키워드 효과) 대비 엔진 점검

대상: `rulebook/general_rule_jp_utf8.txt` 16-1 ~ 16-47. 방법: 코드 정독 + Node 실행 시나리오(실제 `S`/`E`/`Fx` 함수, 실제 게임 상태) + 브라우저(`window.__dbg()`)로 관통 UI 확인.
상태: **수정됨** / **열림** / **문제 없음(검증)** / **의도적 단순화**.

| 룰 번호 | 룰 내용 | 엔진 동작 | 상태 |
|---|---|---|---|
| 16-1/16-2/16-3 | 키워드 효과 정의, 수치/지정 카드만 달라도 같은 종류, 「지정의 카드」 | 해당 없음(정의) | 문제 없음 |
| 16-4-1/16-4-2 | ≪S 어택≫: 체크 매수 증감, 상시형이라 부여 즉시 체크 매수가 바뀜 (체크 도중 얻어도 추가 1장 체크) | 체크 매수(`ctl.total`)를 `beginSecurityCheck`에서 1회만 계산. 1번째 체크의 【시큐리티】 효과로 ≪S 어택+1≫을 얻어도 추가 체크가 없음 | 열림 (`doSecurityStep`이 마지막 체크 후 바로 종료; 체크마다 재계산 + 종료 판정 지연 필요) |
| 16-4-3 | ≪S 어택+1≫ 2개 = +2 (합산은 되지만 ≪S 어택+2≫로 취급 안 함) | `grantKeyword`/`parseStaticGrants` 가 누적 합산 (검증) | 문제 없음 |
| 16-4-4 | 체크 매수가 0 미만이면 0 | `ctl.total`이 -2로 계산되나 `total<=0`이면 체크 안 함 (동작 동일) | 문제 없음 |
| (16-4 관련) | 조건부 상시 「이 디지몬은 ≪S 어택 +1≫을 얻는다」(자신의 턴 등 약 50장: RB1-018, BT11-015, EX5-009 …) | 조건부 상시 부여를 읽는 코드가 없어 보너스 0 | 수정됨 (`parseContGrants`/`genericContGrants`, `s7ContSAttack`) |
| 16-5-1 | ≪블로커≫: 블록 규칙 부여 | 검증 | 문제 없음 |
| 16-5-3 | ≪블로커≫ 여러 개여도 블록은 1회 | UI가 블록 1회로 진행 (검증) | 문제 없음 |
| (16-5/16-9/16-7/16-11 등 관련) | 「<조건> 이 디지몬은 ≪X≫(와 ≪Y≫)를 얻는다」 【자신/상대/서로의 턴】 (블로커·재밍·관통·재기동·속공·돌진·연계 등 약 100개 구절: EX5-049, EX3-039, BT14-086/087, BT15-086/087, BT16-086/087 …) | `hasKeyword`가 조건부 상시 키워드 부여를 못 읽음 (충돌·회피 등 6종만 `hasContinuousKeyword`가 처리, 「A와 B」 2개 나열은 아예 미지원) → 해당 키워드 전부 무효였음 | 수정됨 (`state.js` `parseContGrants`/`genericContGrants`, `s7ContKw` 경유). 지원 조건: 없음 / 명칭·특징 묘사 / 자신의 테이머 / 자신의 「X」 / 자신 트래시 N장 이상 / 상대 패 N장 이하. 그 외 조건(「진화원에 …」「마다」 등)은 열림 |
| 16-6-1/16-6-2 | ≪리커버리≫: 덱 위 N장을 뒷면으로 시큐리티 위에 | 처리 정상. 단 시큐리티가 늘었을 때 이벤트(`securityIncrease`)를 내보내지 않아 「시큐리티가 늘었을 때」 감시 효과가 반응하지 않음 | 수정됨 (`recoverTopOfDeckToSecurity`에 이벤트 추가) |
| 16-7-1 | ≪관통≫: 어택 중 이 디지몬이 배틀에서 상대 디지몬을 「소멸시켰을 때」 체크 | 승리만 판정하고 상대가 실제로 소멸했는지 확인 안 함 (방벽·회피 등으로 상대가 살아남아도 체크). 반대로 동점인데 공격측이 생존 효과로 남고 상대만 소멸한 경우는 체크 안 됨 | 수정됨 (`resolveDigimonBattle`의 `piercing`: 상대가 배틀 에어리어에서 사라짐 && 공격측 잔존 && `defenderWins` 아님) |
| 16-7-3 | ≪관통≫ 체크는 강제 | UI에 "체크 / 안 함" 두 버튼 (선택 가능) | 수정됨 (`main.js` 「안 함」 제거, 「체크」만) |
| 16-7-4 | ≪관통≫은 어택 종료 직전 처리하는 보류 처리. 그 배틀로 유발된 【소멸 시】 효과 해결 후 처리 | 배틀 결과 화면에서 버튼을 누르면 즉시 체크. 소멸 시 효과가 아직 대기 중이어도 체크 가능 | 열림 (대기 효과 해결 전엔 버튼 비활성화 필요) |
| 16-7-5 | 관통을 여러 번 발휘해도 1어택 중 체크는 1회 | 관통 버튼은 배틀 결과 단계에서만 1회 노출 (검증) | 문제 없음 |
| 16-7-6 | 처리 시점에 체크할 디지몬이 없으면 체크 불가 | `stepSecurityCheck`가 어택 디지몬 부재 시 체크 중단 (타 담당자 수정분 포함) + 결과 패널 `last` null 가드 추가 | 수정됨 |
| 16-8 | ≪드로우≫ (N 드로우, 강제) | 컴파일된 `draw` 오퍼 (검증) | 문제 없음 |
| 16-9-1/16-9-2 | ≪재밍≫: 상대 시큐리티 디지몬과의 배틀에서 소멸 안 함 (상시형) | `jammedSurvive` (검증: DP 8000 시큐리티 디지몬에 DP 5000 재밍 생존) | 문제 없음 |
| 16-10-1~16-10-3 | ≪흡수진화 -N≫: 진화 시 자신의 디지몬 1체를 레스트시켜 진화 코스트 -N (레스트는 임의 조건) | UI가 `window.confirm` 후 후보 첫 번째 디지몬 자동 선택 (후보 고르기 없음) | 열림 (후보 선택 UI 없음) |
| 16-10-4 | 1장이 ≪흡수진화≫를 복수로 가지면 중복 유발 | 첫 번째 토큰만 매칭 | 열림 (희귀) |
| 16-10-5 | 진화원이 될(진화하려는) 디지몬 자신도 레스트 대상이 될 수 있음 | 후보에서 진화 중인 디지몬 자신이 제외되어 있었음 | 수정됨 (`absorbEvolveOption`: 액티브면 후보 맨 뒤에 추가) |
| 16-11-1/16-11-2 | ≪재기동≫: 상대 액티브 페이즈에도 액티브 | `engine.nextPhase`에서 처리 (검증). 단 「액티브가 되지 않는다」 계열 효과(`cannotUnsuspendUntil`, `isPreventedFromUnsuspending`)를 무시하고, 조건부 부여/훅 부여 재기동을 못 읽음. 재기동으로 액티브가 될 때 `unsuspend` 이벤트도 누락 | 수정됨 |
| 16-11-3~16-11-5 | 재기동이 여러 개여도 1회, 강제, 턴 플레이어 액티브와 동시 | 검증 | 문제 없음 |
| 16-12-1 | ≪퇴화 N≫: 1~N장 중 플레이어가 원하는 장수를 선언 | 항상 N장(최대) 고정으로 벗김 | 수정됨 (`effects.js` `retreat`: 2 이상이면 `multipleChoice`로 장수 선언, 응답 없으면 N) |
| 16-12-2~16-12-4 | 처리 실행 효과, 0장 불가, Lv.3 카드 이하는 파기 불가 | `retreat()`가 최상단 카드 Lv≤3이면 중단 (검증) | 문제 없음 |
| 16-12-5~16-12-8 | 여러 장 동시 파기(위에서 1장씩), 상단 카테고리가 바뀌어도 선언 장수까지, 별도 퇴화 복수 실행 시 1회마다 상태 확인(효과를 받지 않게 되면 이후 미적용) | 카드별 루프이나 「효과를 받지 않음」 재확인은 `retreat` 진입 시 1회만 | 열림 (희귀) |
| 16-13 | ≪길동무≫: 이 디지몬만 배틀에서 소멸 시 상대 소멸 (강제) | 검증 (공격 승리 시 방어측 길동무, 동점 시 미발동) | 문제 없음 |
| 16-14-1/16-14-2 | ≪디지버스트 N≫: 진화원 N장 파기가 처리 조건 | `costGroup`이 진화원 N장 미만이면 건너뜀 (검증). 어떤 진화원을 파기할지는 자동 선택(파기 시 효과 카드 우선, 없으면 오래된 순) | 열림 (진화원 선택 UI 없음, 의도적 단순화) |
| 16-15 | ≪속공≫: 등장한 턴에도 어택 가능 | `declareAttack` (검증) | 문제 없음 |
| 16-16-1~16-16-5 | ≪진격≫: 메모리가 상대측 1 이상이면 이 디지몬으로 어택할 수 있다 (처리 조건, 발휘 시점의 메모리로 판정, 어택 선언 후 메모리가 바뀌어도 속행) | 진격은 표시용 키워드 부여만 하고 어택 자체는 없음 (`declareAttack`이 메모리를 안 봄) — 【진화 시】 ≪진격≫ 카드 약 12장에서 아무 일도 안 일어남 | 수정됨 (`effects.js` `raid` 오퍼: 메모리 상대측 ≥1 && 액티브 && 확인 후 `startAttack`). 조건 문장 안에 포함된 형태(BT9-068, BT10-070, EX3-063) 및 BT5-017의 「진격으로 어택할 때 액티브 상태 상대에게도」는 열림 |
| 16-17-1~16-17-3 | ≪딜레이≫: 배틀 에어리어에서 이 카드를 파기하여 발휘, 놓은 턴에는 불가 (임의) | `placedTurn` 비교 (검증) | 문제 없음 |
| 16-18-1~16-18-3 | ≪디코이《색》≫: 지정색 다른 자신의 디지몬이 상대 효과로 소멸할 때 이 디지몬을 소멸시켜 방지 (임의) | 상대 효과(`cause==='effect'`)만 대상, 자동 발동(선택 불가) | 열림 (임의 처리인데 확인 없이 자동 발동, 의도적 단순화) |
| 16-19 | ≪아머 해제≫: 소멸 시 겹쳐진 카드 위에서 1장 파기로 소멸 방지 (임의) | 자동 발동 (검증: 위에서 1장) | 열림 (자동 발동, 위와 동일) |
| 16-20 | ≪세이브≫: 테이머 아래에 놓기 (임의) | `saveUnderTamer` 오퍼가 확인 후 놓음 (검증) | 문제 없음 |
| 16-21-1~16-21-6 | ≪머티리얼 세이브 N≫: 소멸 시 디지크로스 조건 카드를 진화원에서 N장 테이머 아래(가장 아래)에 놓음 | 구현 없음 (13장: BT10-009 등) | 수정됨 (`extractMaterialSave`, `deleteStack`에서 호출; 테이머 자동 선택·진화원 위쪽부터 자동 선택은 열림) |
| 16-22 | ≪회피≫: 소멸 시 레스트하여 소멸 방지 (임의) | 자동 발동 | 열림 (자동 발동) |
| 16-23-1~16-23-3 | ≪돌진≫: 어택 시 대상을 최고 DP 액티브 상대 디지몬으로 변경 (임의) | 대상 변경 단계에 버튼 노출 (검증) | 문제 없음 |
| 16-23-4 | 최고 DP 디지몬이 복수면 효과를 발휘한 플레이어가 고름 | `chargeRedirectTarget`이 첫 번째 자동 선택 | 열림 |
| 16-24-1~16-24-5 | ≪연계≫: 다른 디지몬 1체를 레스트해 그 「레스트한 시점의 DP」를 더하고 ≪S 어택+1≫ 획득, 임의 처리 조건 | DP 고정 가산·S어택 +1 (검증). 단 레스트가 효과로 막혀 실패해도 DP/S어택 보너스가 붙음 | 수정됨 (`useChain`: 레스트 실패 시 중단) |
| 16-25 | ≪방벽≫: 배틀 소멸 시 시큐리티 위에서 1장 파기로 방지 (임의) | 배틀에서만, 시큐리티 1장 파기 (검증) / 자동 발동 | 열림 (자동 발동) |
| 16-26 | ≪블래스트 진화≫: 카운터 타이밍에 코스트 없이 진화 (임의) | `blastEvolve` (검증) | 문제 없음 |
| 16-27 | ≪불굴≫: 진화원 있는 이 디지몬 소멸 시 코스트 없이 등장 (강제) | 검증 | 문제 없음 |
| 16-28-1~16-28-3 | ≪마인드 링크≫: 테이머를 진화원에 테이머 카드가 없는 디지몬 진화원 아래에 놓음 (【메인】이면 임의 타이밍) | 【메인】 문장이 빈 스크립트로 컴파일되어 아무 효과 없음 (11장) | 수정됨 (`effects.js` `mindLink`: 조건 디지몬 선택, 테이머 스택을 진화원 맨 아래에) |
| 16-29-1~16-29-4 | ≪파티션《A&B》≫: 자신의 효과·배틀 이외로 배틀 에어리어를 떠날 때 진화원의 지정 카드 1장씩을 코스트 없이 등장 (전부 또는 무발동) | 구현 없음 (13장) | 수정됨 (`extractLeaveSourcePlays`/`playExtractedSources`: 소멸·바운스 경로). 진화원 배정은 그리디, 자동 발동 |
| 16-30-1~16-30-4 | ≪충돌≫: 어택 중 상대 디지몬 전부에 ≪블로커≫ 부여, 가능하면 블록 | 블록 후보에 테이머까지 포함되어 있었음 (「디지몬 전부」만 해당) | 수정됨 (`main.js` `eligibleBlockers`: `category==='digimon'` 필터) |
| 16-31-1~16-31-6 | ≪블래스트 조그레스《「A」+「B」》≫: 지정된 자신의 디지몬 + 패의 지정 카드로 코스트를 지불하고 조그레스 진화, 진화 조건 무시 불가 | `[]`로 컴파일되어 아무 효과 없음 (BT17-078, EX6-011 등 다수) | 수정됨 (`effects.js` `blastJogress`: 카드명 쌍, `canJogress`로 조건 확인, `fuseStacks`). 재료 선택은 자동 첫 후보 |
| 16-32 | ≪스케이프고트≫: 자신의 효과 이외로 소멸 시 다른 디지몬 소멸로 방지 | 다른 디지몬을 첫 번째로 자동 선택 (희생 대상 고르기 없음) | 열림 (선택 UI 없음) |
| 16-33-1~16-33-3 | ≪볼텍스≫: 자신의 턴 종료 시 상대 디지몬에 어택, 이 효과로는 등장한 턴에도 어택 가능 | 이번 턴 등장한 디지몬은 `declareAttack`이 거부해 어택 불가였음 | 수정됨 (`declareAttack` `opts.ignoreEntry`, `shard5.js` 볼텍스 스크립트가 지정) |
| 16-34-1~16-34-3 | ≪오버클럭《X》≫: 자신의 턴 종료 시 토큰 또는 지정 다른 디지몬 1체를 소멸시켜 이 디지몬으로 레스트 없이 플레이어에게 어택 | 구현 없음 (12장) | 수정됨 (`queueTurnEndKeywords` + `overclock` 오퍼, `startAttack(...'PLAYER',{noRest:true})`). 테이머 형 「≪오버클럭≫」(EX11-060)은 열림 |
| 16-35-1~16-35-4 | ≪빙장≫: 시큐리티 디지몬 이외와의 배틀에서 진화원 장수 비교 (많으면 승, 적으면 패, 같으면 양쪽 패) | `resolveDigimonBattle`에서 진화원 장수 비교 (검증) | 문제 없음 |
| 16-36-1~16-36-3 | ≪디코드《X》≫: 배틀 이외로 배틀 에어리어를 떠날 때 진화원에서 지정 디지몬 1장을 코스트 없이 등장 (임의) | 구현 없음 (13장) | 수정됨 (`extractLeaveSourcePlays`; 자동 발동, 진화원 위쪽 후보 우선). 테이머형 ≪디코드≫(EX11-058)는 열림 |
| 16-37-1~16-37-3 | ≪프래그먼트《N》≫: 진화원을 N장 파기하여 소멸 방지 | 진화원이 N장 미만이어도 있는 만큼 파기하고 소멸 방지 | 수정됨 (`trySurviveByKeyword`: 진화원 ≥ N 필요). 파기할 카드 선택은 자동 (열림) |
| 16-38 | ≪에그제큐트≫ (턴 종료 시 어택, 어택 종료 시 소멸, 보류 처리) | 타 담당 구현 (`s8_execute`) | 문제 없음 |
| 16-39-1~16-39-3 | ≪프로그레스≫: 어택 중 이 디지몬은 상대의 효과를 받지 않음 | 구현 없음 (10장) | 수정됨 (`effectBlocked`에 `attackCtx` 기반 판정). `effectBlocked`를 거치지 않는 효과 경로는 열림 |
| 16-40-1~16-40-3 | ≪링크+N≫: 링크 카드 상한 증가, 복수 합산 | `《링크 +1》`의 공백 때문에 정규식이 안 맞아 상한이 항상 1이었음 (BT21-101, EX11-073, BT26-086 등 10장) | 수정됨 (`parseStaticGrants` 정규식 `링크\s*\+`) |
| 16-41 | ≪트레이닝≫ (메인, 레스트, 육성 에어리어 가능) | 버튼 + `useTraining` (검증) | 문제 없음 |
| 16-42-1~16-42-3 | ≪사용조건《X》≫: 지정 카드(에어리어의 디지몬/테이머)로 색 조건 무시 | 「이 카드는 색 조건을 무시할 수 있다」 문구만 처리, ≪사용조건≫(33장)은 무시됨 → 색이 안 맞으면 옵션 사용 불가 | 수정됨 (`optionColorOk`: `사용조건` 파싱 + 배틀 에어리어 매칭) |
| 16-43 | ≪천승≫: 소멸했을 때 시큐리티 위에 놓기 (임의) | 타 담당 구현 (`s8_tensho`) | 문제 없음 |
| 16-44-1~16-44-3 | ≪급습≫: 자신의 턴 종료 시 이 디지몬으로 어택 (임의) | 구현 없음 (EX12-060 등 4장) | 수정됨 (`queueTurnEndKeywords` + `ambush` 오퍼) |
| 16-45-1~16-45-3 | ≪수호≫: 다른 자신의 디지몬이 상대 효과로 배틀 에어리어를 떠날 때 이 디지몬을 소멸시켜 방지 (임의) | 구현 없음 (EX12-056/057) | 수정됨 (`tryDecoy`에 수호 분기, 소멸 경로만; 바운스 경로·옵션의 「수호를 얻는다」는 열림, 자동 발동) |
| 16-46-1~16-46-3 | ≪분리《X》≫: 자신의 효과 이외로 떠날 때 지정 링크 카드 1장을 파기하여 방지 | 구현 없음 (BT26-010 등 7장) | 수정됨 (`trySurviveByKeyword`; 자동 발동) |
| 16-47-1/16-47-2 | ≪계승《X》≫: 진화원 중 가장 위의 지정 카드의 ≪계승≫ 이외의 효과 전부를 얻음 | 구현 없음 (BT26-032/080/103/060) | 수정됨 (`inheritKeywordSource`: `stackContributors`/`queueTriggersForStack`에 반영) |
| (공통) | 임의 처리 조건이 있는 생존/방지형 키워드(회피·아머 해제·방벽·스케이프고트·프래그먼트·디코이·수호·분리·디코드·파티션·머티리얼 세이브)는 플레이어가 사용 여부와 순서를 고를 수 있음 | `deleteStack`이 동기 함수라 확인 창 없이 정해진 우선순위(디코이 → 희생 → 회피 → 아머 해제 → 프래그먼트 → 방벽 → 분리 → 스케이프고트 → 카드 고유 능력)로 자동 발동 | 열림 (비동기 선택 경로 필요, 큰 작업) |

## 요약
- 확인한 룰: 제16장 16-1 ~ 16-47 (하위 조항 포함 약 135개), 브라우저에서 ≪관통≫ UI 확인 (강제 체크 버튼 / 체크 후 시큐리티 감소) 완료.
- 발견한 모순/누락: 약 35건. 수정: 약 27건 (미구현 키워드 12종 = 디코드·파티션·분리·수호·계승·프로그레스·오버클럭·급습·머티리얼 세이브·마인드 링크·블래스트 조그레스·사용조건, 진격 오퍼, 조건부 키워드 부여 약 100구절, 링크+ 정규식 등). 열림: 약 8건 (S어택 체크 도중 획득, 관통 타이밍, 자동 발동/선택 없는 임의 효과 일괄, 돌진 동률 선택, 흡수진화 후보 선택, 진격 조건 내포형, 퇴화 재확인, 테이머형 디코드/오버클럭 등).
- 회귀 검증: `node scripts/soak.mjs 30` 고유 오류 0, `node scripts/audit-effects.mjs` 커버리지 100%.
