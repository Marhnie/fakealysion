# 룰 대조 기록: 1장(게임 개요) ~ 5장(게임 준비)

`rulebook/general_rule_jp_utf8.txt` (Ver.4.2) 1~5장을 규칙 단위로 읽고, 엔진(src/state.js, engine.js, effects.js, main.js, deckbuilder.js)을 코드 리딩 + Node 실행(실제 S/E 함수, 실제 게임 상태)으로 대조한 결과.
상태: **수정됨** / **열림** / **정상**(검증만 함, 모순 없음).

## A. 모순 발견 (수정됨)

| 룰 번호 | 룰 내용 | 엔진 동작 | 상태 |
|---|---|---|---|
| 5-2-1-6 | 시큐리티 세팅: 덱 위에서 1장씩 놓아 **덱의 1번째 카드가 시큐리티의 맨 아래**가 된다 | `pl.security = deck.splice(0,5)` — 덱의 1번째 카드가 시큐리티 맨 위(첫 체크 대상)가 됨 (순서 반대). 실행으로 확인 | 수정됨 (engine.setSecurityStacks: `.reverse()`) |
| 5-2-1-3/4 | 선공(가위바위보)은 **핸드 뽑기·멀리건 이전에** 결정, 멀리건은 선공부터 순서대로 1회씩 | 멀리건이 끝난 뒤 코인플립으로 선공 결정. 두 플레이어가 순서 무관하게 동시에 결정 가능 | 수정됨 (main.js startNewGame에서 선공 선결정, 후공 패널은 선공이 결정할 때까지 버튼 비활성) |
| 1-2-4 / 1-2-5 | 모든 플레이어는 언제든 투항 가능, 투항은 즉시 패배이며 효과를 유발/발휘하지 않음 | 투항 기능 자체가 없음 | 수정됨 (engine.surrender + 상단바 P1/P2 투항 버튼, 확인창) |
| 1-3-11-1 / 4-2-2 | 코스트를 지불할 수 없는 카드는 실행(등장·진화·사용·링크)을 선언할 수 없다. 지불은 정확히 그 수치 | 지불 가능 여부 검사가 전혀 없음. `spendMemory`가 ±10에서 조용히 clamp 하여 메모리 이상의 코스트도 통과 | 수정됨 (`S.canPayCost` = 자기 쪽 메모리+10 이상 불가; useOptionCard/digivolve/linkCardTo/fuseStacks 및 main.js 등장에 적용) |
| 1-2-3-1 / 16-4 | 「1장 이상 체크할 수 있는 디지몬」의 어택이 성립하고 시큐리티 0장일 때만 승리. ≪S어택-1≫로 체크 장수가 0이면 체크하지 않음 | 체크 장수(total)가 0 이하여도 1장 체크 → 시큐리티 0장이면 승리로 처리 | 수정됨 (stepSecurityCheck: total<=0이면 체크 없이 종료) |
| 4-17-2 | 이동은 **DP를 가진** 디지몬만 가능 | Lv.3 이상이면 이동 허용(DP 없는 BT18-086 루체몬:라르바도 이동), DP를 가진 Lv0 디지타마 카드(EX2-007)는 이동 불가 | 수정됨 (`S.canMoveFromRaising`, state/engine/main 3곳) |
| 3-1-3-9 | 디지타마 카드가 패/시큐리티/덱 등 비공개 영역에 놓이면 **디지타마 덱 맨 아래**로 | 패·시큐리티에 그대로 들어감(바운스 등) | 수정됨 (`S.normalizeDigitamaZones`를 autoAdvance 룰체크로 실행). 실행 확인 |
| 3-4-7-4 | 육성 에어리어 카드의 효과는 육성 에어리어를 지정/참조하지 않는 한 유발·발휘하지 않는다 | `[육성]` 표기 없는 【자신의 턴 종료 시】·【등장 시】·메인 페이즈 개시 등은 육성 에어리어에서도 유발(진화 시만 예외 처리돼 있었음). 진화원 효과·커스텀 훅(activeHooks)도 무제한 | 수정됨 (queueTriggersFor/queueInheritedTriggersFor/activeHooks에서 [육성] 표기 없는 세그먼트 제외) |
| 3-4-7-5 / 3-4-7-8 | 육성 에어리어를 지정하지 않는 효과는 그 카드를 선택·참조할 수 없다 | 퇴화/액티브화/키워드 부여/배틀 불사 부여의 대상 선택지에 (상대 포함) 육성 에어리어 카드가 포함. 옵션 색조건 무시 조건(아머체 등)이 육성 카드를 셈 | 수정됨 (effects.js 4곳, state.optionColorOk) |
| 2-5-3 | DP를 가지지 않는 카드는 DP를 가산/감산할 수 없다 | DP 없는 디지몬에 DP+3000 → 유효 DP 3000, DP-는 0 이하로 룰체크 소멸도 가능 | 수정됨 (`S.stackHasDP`, modifyDP/s7AddDpMod/ruleCheckDP) |
| 4-21-3 | 토큰 위에는 카드를 겹칠 수 없다(진화 불가) | 진화 조건이 레벨/색 무관한 경우 토큰에 진화 가능 | 수정됨 (canEvolveAny에서 토큰 호스트 거부) |
| 4-4-2 | 테이머 아래에 (세이브 등으로) 놓을 때 지정이 없으면 겹친 카드의 **맨 아래**에 | saveCardUnderTamer/테이머 아래 놓기가 `sources.push`(맨 위) | 수정됨 (unshift; sources[0]=맨 아래 규약) |
| 1-3-10 | 셔플 시 공개 정보(앞면 시큐리티)는 비공개로 만든 뒤 셔플 | 시큐리티 셔플 후에도 앞면 표식(secUp)이 유지 | 수정됨 (shuffleSecurity에서 secUp 초기화) |
| 3-1-2-1 / 3-6-3 | 트래시는 공개 영역: 내용과 순서를 언제든 확인 가능 | UI가 장수만 표시, 내용 열람 불가 | 수정됨 (트래시 더미 클릭 = 목록 펼침, 위→아래 순) |
| 1-4-1-2-2 / 2-3-4-6 | 같은 카드번호 4장까지, 〈룰〉로 N장까지 허용되는 카드(BT6-085 등 50장) 예외 | 덱빌더가 항상 4장으로 고정, 저장된 덱의 장수 검증 없음 | 수정됨 (maxCopiesFor가 〈룰〉 문구 파싱, validate에 카드별 상한 검사) |
| 4-9-5 (부수) | 링크 가능 여부 확인 후에 기존 링크 카드를 파기 | 패에 링크 카드가 없어 실패해도 기존 링크 카드를 먼저 파기해 버림 | 수정됨 (linkCardTo 선검사) |

## B. 모순 발견 (열림)

| 룰 번호 | 룰 내용 | 엔진 동작 | 상태 |
|---|---|---|---|
| 2-3-1-4 | 명칭 속 「ACE」는 명칭에 포함되지 않는다 (「메탈그레이몬」 지정이 「메탈그레이몬 ACE」에도 해당) | 명칭 비교가 `nameKo` 문자열 그대로(진화 조건 nameExact, 이름 지정 필터, 조그레스 등 수십 곳). 데이터에 ACE 명칭 83장. 중앙 정규화 필요(표시명과 비교명 분리) | 수정됨 — state.loadData가 「ACE」를 명칭에서 제거(nameKo=비교명, 표시명은 nameDisplayKo). 카드칩/덱빌더 검색은 표시명 사용 |
| 2-3-4-5 / 2-3-4-6 | 〈룰〉카드번호: 「X」로도 취급 + 덱에 「X」와 합계 4장까지 | 덱빌더는 카드번호별 개수만 셈(별칭 합산 안 함), 카드번호 별칭 효과 참조도 미구현 | 수정됨 — state.cardNumberAlias/deckLimitKey/copiesTowardLimit + deckLegality: 별칭 카드는 합계로 상한 계산(덱빌더 추가/검증) |
| 1-4-1-2-1 / 1-4-1-3-1 | 덱은 정확히 50장, 디지타마 덱은 5장 이하 | 검증은 덱빌더 화면 표시뿐. 부족/초과 덱도 저장·게임 시작 가능(연습용 의도일 수 있음) | 수정됨 — state.deckLegality 중앙 검증(50장/디지타마 ≤5/상한/구역). 덱빌더 저장 차단 + 새 게임 시작 차단(메시지 표시). soak처럼 newGame 직접 호출은 영향 없음 |
| 1-3-6 | 효과로 카드를 고를 때 반드시 1장 이상 선택(0장 불가) | 대다수 선택 UI가 null(선택 안 함) 허용, 엔진도 `if (pick)`로 무시 | 수정됨 — 효과 텍스트가 임의(수 있다/까지)가 아니면 payload.required=true → 후보가 있을 때 취소 버튼 숨김(effects.wrapChoose + renderUiChoice). 엔진 if(pick) 무시는 임의 효과용으로 유지 |
| 3-1-3-4 / 4-19-5 | 동시에 여러 카드가 영역을 벗어날 때 놓는 순서는 영향 원 플레이어가 선택, ≪오버플로우≫ 동시 처리는 턴 플레이어 우선 후 1개씩 선택 | 순서 선택 UI 없음. 오버플로우는 스택 내 카드 순서대로 즉시 처리(메모리 증감이라 결과는 대체로 동일) | 수정됨 — 공개 카드 덱 되돌림 순서 선택(orderCards, S.resolveRevealOrdered). 오버플로우 동시 처리는 메모리 증감이라 결과 동일하여 순서 UI 없음, 그 외 카드별 동시 배치는 미적용(부분) |
| 4-9-5 | 링크 상한 초과 시 기존 링크 카드 중 파기할 카드를 플레이어가 선택 | 가장 오래된 것부터 자동 파기 | 수정됨 — S.linkDiscardIdx + pickLinkCard 선택 UI (UI 드롭, shard5/6/8 링크). shard7 linkFrom(동기)은 오래된 것 자동 |
| 4-6 / 4-20 | 듀얼 카드(디지몬+옵션), 아츠 진화 | 데이터에 듀얼 카드 0장, artsDigivolve는 미연결 스텁 | 의도적 단순화 — 데이터에 듀얼 카드가 0장이라 `artsDigivolve`는 스텁 유지. |
| 4-17-5 | 이동한 디지몬의 효과 상태는 이어지지 않는다(발휘 대기 효과가 사라짐) | 이동 시 이미 큐에 있는 대기 효과를 취소하지 않음 | 수정됨 — `cancelWaitingEffectsOf`: 육성↔배틀 이동 시 그 디지몬의 발동 대기 효과를 취소(소멸 시 제외). |
| 16-4-2 | ≪S어택≫은 부여 즉시 체크 장수 증감 (체크 도중 획득해도 추가 체크) | `beginSecurityCheck`가 시작 시점 total을 고정(체크 도중 부여/제거 반영 안 됨) | 수정됨 — 체크 시작 시·체크 후·단계 사이마다 ≪S 어택≫ 장수를 재계산(`stepSecurityCheck`/`battleSecurityCheck`). |
| 3-4-7-3 | 육성 에어리어 카드는 육성 에어리어를 지정하지 않는 효과를 받지 않는다 | 이번에 손본 선택 대상 4곳 외에 shard 카드별 스크립트·전역 DP 부여 훅이 `[raising, ...battle]`을 전체 대상으로 취급하는 곳이 남아 있을 수 있음(카드별 감사 필요) | 수정됨 — 감사 결과: 카드별 「자신의 「X」」 선택 2곳(BT19-078, BT22-095)이 육성 에어리어를 포함하던 것을 배틀 에어리어 한정으로 수정, 나머지는 uid 조회용 헬퍼이거나 육성 에어리어를 명시한 카드. |
| 1-3-11-3/4, 8-x | 대체 코스트(진화 코스트 증감 효과 선택 등) 소비 후 지불 불가 시 효과가 소모됨 | main.js 진화 드롭: consumeEvoCostMod/확인창이 지불 검사보다 먼저 실행 → 지불 불가로 거부되어도 1회성 코스트 감소 효과가 소모될 수 있음 | 수정됨 — 진화 드롭이 1회성 코스트 증감 소비 전에 스냅샷을 잡고(`snapshotEvoCostMods`) 거부·실패 시 복원(`restoreEvoCostMods`); 조그레스/버스트/앱합체/효과 경로 동일. |

## C. 검증 완료 (모순 없음)

| 룰 번호 | 확인 내용 | 상태 |
|---|---|---|
| 1-1-1, 1-2-1/2 | 2인전, 승리 즉시 종료 (state.winner) | 정상 |
| 1-2-3-2 | 드로우 페이즈에 덱 0장이면 패배(효과 드로우는 패배 아님) — 실행 확인 | 정상 |
| 1-2-6 | 효과로 승리(BT17-100 s4_win) 즉시 반영 | 정상 |
| 1-3-3 | 이미 레스트인 카드를 다시 레스트해도 이벤트 1회만 — 실행 확인 | 정상 |
| 1-3-8 / 1-3-9 | DP 증감은 합산 후 원래 값에 가산, 코스트 감소는 0 미만이 되지 않음 (Math.max(0,…)) | 정상 |
| 1-3-10 | 셔플은 Fisher-Yates 무작위 | 정상 |
| 1-4-2 / 4-1 | 메모리 ±10 clamp, 부호 규약(p1 양수), 「X 이하」「상대 쪽 X 이상」 비교식 (effects.js parseCondition) | 정상 |
| 2-3-4-2/3/4 | 〈룰〉특징/명칭 별칭 처리(state.js, shard2) | 정상 |
| 3-4-3, 4-13 | 에어리어에 놓이는 카드는 액티브 상태 (makeStack) | 정상 |
| 3-4-7-2, 4-18 | 육성 에어리어 1장, 부화는 비어 있을 때·디지타마 덱 있을 때·육성 페이즈 1회 | 정상 |
| 3-7-1~5, 4-5 | 시큐리티 비공개·순서 고정, 체크한 카드 트래시로 | 정상 |
| 4-7-6 | 퇴화 시 레스트 상태 승계(같은 스택 유지) | 정상 |
| 4-7-8 | 스택이 벗어나면 진화원·링크 카드 동시 트래시(deleteStack, 바운스) | 정상 |
| 4-9-4/6 | 새 카드가 되면(진화/조그레스/퇴화) 링크 카드 파기 | 정상 |
| 4-15/4-16 | 소멸=트래시행, 【소멸 시】는 소멸 영역에서 유발 | 정상 |
| 4-19-1~4 | ≪오버플로우≫: 에어리어를 벗어날 때/진화원에서 벗어날 때만, 링크·진화로 겹쳐질 때는 미발동 | 정상 |
| 4-21-5 | 토큰은 영역을 벗어나면 게임에서 제거(purgeTokens) | 정상 |
| 4-22 | 옵션 색조건(다색은 전 색, 다색 디지몬은 복수 색 충족, 육성 에어리어 포함) | 정상 |
| 5-2-1-1/2 | 덱·디지타마 덱 셔플 | 정상 |
| 5-2-1-4/5 | 시작 핸드 5장, 멀리건 1회(핸드 전부 덱에 넣고 셔플, 5장 다시) | 정상 |
| 5-2-1-7/8 | 메모리 0에서 시작, 선공 턴부터 시작 | 정상 |
| 6-3-1-1 | 선공 첫 턴 드로우 없음 (참고: 6장 담당) | 정상 |

검증: `node scripts/soak.mjs 30` distinct errors 0, `node scripts/audit-effects.mjs` coverage 100%. 브라우저에서 멀리건 순서 게이팅·투항 버튼·트래시 열람 동작 확인.
