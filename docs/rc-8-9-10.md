# 룰북 Ver.4.2 8장(진화)·9장(사용)·10장(링크) 대조 결과

검증 방법: 코드 정독 + Node에서 실제 S/E 함수로 실행(빈 덱 진화, 조그레스 파싱 71장, 링크 조건 검사 등). 상태: 수정됨 / 열림 / 의도적 단순화 / 문제 없음.

| 룰 번호 | 룰 내용 | 엔진 동작 | 상태 |
|---|---|---|---|
| 8-1-1 | 진화는 자신의 디지몬 카드를 공개해 에리어의 자신의 카드(디지몬) 위에 겹침. 메인 페이즈 행동 | 진화 대상 스택이 테이머/옵션이어도 UI가 시도 가능(레벨 조건 때문에 실제로는 거부되나 명시 검사 없음) | 수정됨 (main.js 진화 드롭에 category!=='digimon' 거부 추가) |
| 8-1-2-1 | 진화 조건이 여러 개면 플레이어가 하나를 고름 | E.evolutionMethods가 만족하는 모든 방법(일반/〔진화〕 특수 라인/버스트/어플 합체/효과 부여 대체 진화)을 나열. 2개 이상이거나 부작용(버스트·어플)이 있으면 모달 선택창(기본 코스트 + S.previewEvoCostDelta 반영 후 코스트 표시, 취소 시 일회성 할인 미소모). 같은 코스트의 일반 방법은 병합. 선택한 방법의 기본 코스트에 수정 효과가 정확히 1회 적용됨. 효과 진화(evolveEffect)의 "진화 코스트 (-N)"도 조건이 여러 개면 선택, 고정 코스트/코스트 없음은 선택 불필요. canEvolveAny는 기존처럼 최저 코스트 반환 | 수정됨 |
| 8-1-2-2 | 진화 조건 무시 진화는 조건과 무관 | evolveEffect의 ignoreCond가 조건뿐 아니라 「진화할 수 없다」 제한(evolveTargetRestriction)까지 함께 건너뜀 | 수정됨 (E.evoRestrictionCheck 신설; 모든 ignoreCond 경로(effects/shard1~8)가 제한을 존중) |
| 8-1-2-3 | 진화하는 카드는 진화 전의 표시 형식(레스트) 승계 | 같은 스택에 카드만 덮으므로 레스트 상태 유지 | 문제 없음 |
| 8-1-2-4 | 진화 시 유발하는 즉시형 효과는 카드 공개 직후 유발 | 공개 단계 개념 없음, 진화 후 일반 【진화 시】와 동일 타이밍 | 의도적 단순화 |
| 8-1-2-5 | 진화는 1회에 1체만 | UI/엔진 모두 1체씩 | 문제 없음 |
| 8-1-2-6 | 공개 후 진화 불가 시 카드 복귀, 코스트 미지불이면 메모리 불변 | 조건 불일치 시 로그만 남기고 아무것도 소모하지 않음 | 문제 없음 |
| 8-1-2-7 | 진화는 진화원이 '늘어난 것'으로 간주하지 않음 | 진화원 증가 이벤트를 별도로 내보내지 않음(진화원 추가 감시 효과 없음) | 문제 없음 |
| 8-1-2-8 | 드로우 불가(덱 0장)여도 진화 가능, 드로우 없이 해결 | 덱 0장으로 digivolve 실행 시 패배 없이 진화 완료(검증) | 문제 없음 |
| 8-1-3 | 진화 절차: 선언/공개, 조건 선택, 코스트 지불, 겹치고 1장 드로우 | digivolve: 카드 겹침 → 코스트 지불 → 드로우 → 트리거 | 문제 없음 |
| 8-2-1 | 조그레스: 조건이 지정한 '디지몬' 복수를 재료로 함 | canJogress가 재료의 카테고리를 확인하지 않아 색 조건만 있는 조그레스에 테이머도 재료가 될 수 있었음 | 수정됨 (canJogress에 둘 다 digimon 검사) |
| 8-2-2-1-1 | 조그레스 결과는 액티브로 등장 | fuseStacks가 suspended=false | 문제 없음 |
| 8-2-2-1-2 | 소재 링크 카드는 겹치기 직전 파기 | discardLinkCardsOnNewCard 호출 | 문제 없음 |
| 8-2-2-1-3/4 | 어택 중/어택 대상이던 소재가 조그레스하면 어택자/대상 없음 | 메인 페이즈 busy 차단으로 UI에서 도달 불가. 효과로 조그레스(jogressEffect)할 경우 어택 스택이 uid로 남아 있을 가능성(미검증) | 수정됨 (fuseStacks가 state.attackCtx의 어택자/대상이 소재면 어택을 종료) |
| 8-2-2-1-5/6 | 부여 효과·[턴 X회] 카운트 초기화 | 새 스택 생성으로 자연 초기화 | 문제 없음 |
| 8-2-2-1-7 | 등장 턴 디지몬에서 진화해도 그 턴부터 어택 가능 | attackEligibleTurn = 현재 턴 | 문제 없음 |
| 8-2-2-2 | 조건 왼쪽 카드가 위 | fuseStacks가 좌/우 판정(#4 수정 유지) | 문제 없음 |
| 8-2-2-3 | 소재 각자의 진화원 순서 유지 | bottom.sources, bottom, top.sources, top | 문제 없음 |
| 8-2-2-4 | 효과로 조그레스는 조그레스 효과에서만 | 진화 효과(evolveEffect)는 조그레스 대상 불가(별도 jogressEffect) | 문제 없음 |
| 8-2-2-5 | 진화 코스트 증감 효과가 조그레스에도 적용 (docs #5) | 기존: 사용자 입력 코스트만 사용. 이제 카드에 인쇄된 조그레스 코스트를 자동 사용하고 consumeEvoCostMod/continuousEvoCostDiscount/hookEvoCostDiscount 적용(파싱 불가 줄만 수동 입력). 기준 스택은 드롭 대상 스택 | 수정됨 (UI 경로·효과 경로 `jogressEffect` 모두 인쇄 코스트에 `consumeEvoCostMod`/`continuousEvoCostDiscount`/`hookEvoCostDiscount` 적용, 실패 시 복원) |
| 8-2-3 | 조그레스 절차 (코스트 지불 후 겹치고 1장 드로우) | fuseStacks 동일 | 문제 없음 |
| 8-3 (8-3-1~8-3-3) | 버스트 진화: 테이머를 패로 되돌려 진화, 진화한 턴 종료 시 위 카드 1장 파기(보류 처리), 효과로는 버스트 진화 효과로만 가능 | 카드 6장(BT13-020/033/060 등) 존재하나 엔진·UI 모두 미구현. 〔버스트 진화〕 줄은 일반 진화 조건으로 파싱되지 않아 진화 불가 | 수정됨 (S.parseBurstEvolution/burstCheck/burstEvolve + 드롭 UI(main.js), 턴 종료 시 위 카드 1장 파기 예약) |
| 8-3-2-3 / 8-4-2-3 | 진화 영향 효과는 버스트/앱합체에도 적용 | 두 진화 자체가 없음 | 수정됨 (버스트/앱합체 코스트에도 consume/continuous/hook 진화 코스트 증감 적용) |
| 8-4 (8-4-1~8-4-3) | 앱합체: 2종 조합으로 링크된 카드의 링크 카드를 위에 겹쳐 진화 | 카드 27장(BT21-018/023 등) 미구현. 〔어플 합체〕 진화 불가 | 수정됨 (S.parseAppFusion/appFusionCheck/appFusion + 드롭 UI; shard6·shard8 효과 경로와 링크 테이머 BT21-084/BT22-087/BT24-087 스크립트가 공용 함수 사용) |
| 9-1-1 | 사용은 색 조건 충족 필요(4-22) | optionColorOk (#3 수정 유지) | 문제 없음 |
| 9-1-2 / 9-1-6 | 사용과 발휘 사이에 다른 효과·룰체크 끼어들지 않음, 사용 시 유발 즉시형은 공개 직후 | 사용 → 코스트 지불 → pending 큐에 【메인】 등록. 다른 pending과 섞여 처리 순서 선택이 가능할 수 있음 | 의도적 단순화 |
| 9-1-3 | 【메인】 효과에 조건이 있으면 충족했을 때만 발휘, 사용은 유효 | 조건 미충족이어도 코스트 지불 후 사용됨(옳음) | 문제 없음 |
| 9-1-4 / 9-1-5 | 사용 중인 옵션은 어느 영역에도 속하지 않고, 【메인】 해결 후 트래시로 | useOptionCard가 사용 즉시 트래시에 넣음(효과 해결 중 트래시 장수 참조 효과에서 미세한 차이, "이 카드를 패에 추가/놓는다" 계열은 전용 함수로 보정) | 의도적 단순화 |
| 9-1-7 | 사용은 1장씩 | 1장씩 | 문제 없음 |
| 9-1-8 | 공개 후 사용 불가 시 카드 복귀, 메모리 불변 | 색 조건/사용 금지 시 hand.splice 이전에 종료되어 복귀·메모리 불변 | 문제 없음 |
| 9-1-9 | 절차: 선언·공개, 사용 코스트 지불, 【메인】 발휘 | 동일 순서 | 문제 없음 |
| 10-1-1 | 링크는 「링크」를 가진 카드를, 링크 조건이 지정한 자신의 디지몬에 손패/배틀 에리어에서 붙임. 링크 조건·코스트는 링크되는 카드 자신에게 인쇄 | 기존: 진화원이 링크 슬롯을 '부여'한다고 오해하여 (1) 링크 능력 없는 손패 카드도 드롭 가능 (2) 대상 조건 미검사 (3) 코스트를 진화원 것으로 지불. 이제 S.linkCheck로 링크 카드 자신의 「링크: 조건: 코스트 N」을 파싱해 대상 조건(특징 등)과 코스트 검사, 배지는 모든 자신의 배틀 디지몬에 표시 | 수정됨 (UI 경로 `S.linkCheck`; 효과 경로 shard5/6/7/8은 각자 `linkCheck`/`canLink`/`canLinkTo`로 조건을 검사한 뒤 `linkCardTo` 호출을 확인함; 링크 상한 초과 시 파기 선택은 비동기 `linkDiscardIdx`(shard7 `linkFrom` 포함)) |
| 10-1-1 | 배틀 에리어의 디지몬(링크 카드)을 다른 디지몬에 링크하는 행동 | 손패에서 링크만 지원, 배틀 에리어→디지몬 링크 UI 없음 | 수정됨 (S.linkFromBattle + 링크 배지에 배틀 에리어 스택 드롭 (main.js)) |
| 10-1-2-1 | 겹쳐진 카드가 있으면 가장 아래에 끼움 | linkCards를 별도 배열로 관리(진화원 아님) | 의도적 단순화 |
| 10-1-2-2 | 링크된 카드는 링크 카드가 됨 | stack.linkCards | 문제 없음 |
| 10-1-2-3 | 링크 시 유발하는 즉시형 효과는 공개·대상 선택 직후 | 링크 후 일반 【링크 시】로 처리 | 의도적 단순화 |
| 10-1-3 | 절차: 선언·공개, 조건 선택+대상 선택, 코스트 지불, 끼움 | linkCardTo: 코스트 지불 → 끼움 → 트리거 | 문제 없음 |
| 10-4-1 (참조 4-9) | 호스트가 새 카드가 되면(진화/조그레스) 링크 카드 파기 | digivolve/fuseStacks/artsDigivolve에서 discardLinkCardsOnNewCard | 문제 없음 |
| 링크 UI | 육성 에리어의 디지몬에 링크 | 배지를 배틀 에리어에만 표시(룰상 조건이 허용하면 가능) | 수정됨 — 육성 에어리어의 디지몬에도 🔗 배지 표시(링크 조건이 허용할 때 드롭 가능) |

수정 파일: src/main.js(진화 드롭 카테고리 검사, 조그레스 코스트 자동/수정 적용, 링크 드롭 검증), src/state.js(canJogress 카테고리 검사, linkCheck 추가), src/effects.js(evolveEffect에 진화 코스트 증감 적용).
