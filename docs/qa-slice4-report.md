# 공식 카드별 Q&A 검증 보고서 — slice4 (Q id 3996 ~ 5168)

원문(일본어 Q&A)은 `data/rulings/slice4.json`(gitignore)에만 있고, 이 문서와 테스트 파일에는 Q id와 자체 요약만 둔다.
분류표: `docs/qa-slice4-classification.json` ({qid: 'T'|'R'|'N'}). 테스트: `scripts/qa/qa-slice4-{a..h}.mjs` (+ 공용 `scripts/qa/lib-s4.mjs`).
실행: `node scripts/qa/qa-slice4-a.mjs < /dev/null` (a~h 각각, 실패 시 exit 1). `QA_OUT=<파일>`을 주면 시나리오별 결과를 jsonl로 남긴다.

## 1. 수치

| 항목 | 개수 |
|---|---|
| 전체 Q&A | 1113 (카드 505종, 전부 한국판 DB에 존재) |
| TESTABLE (T) | 950 |
| RULE-ONLY (R) | 149 (정의/설명형, "기술이 있는 카드란", 누가 결정하는가 등) |
| NOT-APPLICABLE (N) | 14 (한국판 텍스트와 다름 4117-4119 / UI 계층 규칙 — 어택 중 새 어택 선언 불가 계열 4100·4298·4346·4362·4419·4431·4528·4594·4598·4728·4913) |
| 시나리오 구현 (distinct Q id) | 158 (시나리오 163개; 같은 Q를 정/부 두 방향으로 확인한 경우 포함) |
| 통과 (최종) | 158 / 158 |
| 실패 → 엔진 수정 | 아래 8건 (Q id 기준 약 28개) |
| 실패 → 미해결 | 0 |
| 시나리오 해석 오류로 판명 (엔진 정상) | 5건 (4124, 4575, 4180, 4289, 4027) + 1건 철회(4242, Lv.- 검증 불가) |

주의: T=950은 "원리상 시나리오로 옮길 수 있음"의 넓은 분류다. 같은 문장이 카드만 바꿔 반복되는 항목(표향 시큐리티, 효과를 받지 않는다 등)이 많아 실제 고유 판정은 이보다 훨씬 적다. 구현한 것은 그중 158개(약 17%)이며 우선순위는 효과 해결·비용·대상·진화(딜레이/트레이닝/특수 진화)·치환/면역 순이었다.

## 2. 수정한 엔진 버그

1. **LM-033~038 (Q4063-4074) — "이 카드는 ○○로도 색 조건을 만족시킬 수 있다" 미구현.** 증상: 추가 색의 디지몬/테이머(육성 에어리어 포함)가 있어도 옵션 사용 불가. 원인: `optionColorOk`가 카드 자체 색만 검사. 수정: `src/state.js` optionColorOk에 해당 문구 파싱 추가, `src/decktools.js`의 덱 검사(option-color)도 동일 반영.
2. **EX9-021 (Q4765-4767) — 【어택 종료 시】 미구현(수동 처리 안내만 출력).** 수정: 새 `src/cards/shard64.js`에 스크립트(진화원에서 「그레이몬」/Ver.1 1장 + 「가루몬」/Ver.2 1장 코스트 없이 등장, 가능한 만큼, 등장했다면 이 디지몬을 시큐리티 위로).
3. **BT22-007 (Q4859/4860) — 마더 이터.** 원인 3중: (a) 진화원의 「마더 이터」가 3장 미만이면 발휘 불가(공식: 가능한 만큼), (b) 디지타마 카드는 무조건 효과 등장 불가 처리, (c) DP 없음 → 룰체크 파기(17-1-3-2-1)가 [육성] "DP 16000으로 취급"을 무시. 수정: shard64 스크립트 재작성(최대 3장, 있는 만큼), `state.js` playFreeFromZone에서 자체 【등장 시】가 있는 디지타마는 예외, `stackHasDP`가 육성 에어리어 "DP N으로 취급" 문구를 인정.
4. **RB1-009 (Q4080) — "진화원에 명칭에 「감마몬」을 포함하는 카드가 있는 자신의 「감마몬」은 진화 조건 무시, 코스트 3" 미구현.** 수정: `state.js` evolveTargetRestriction에 패턴 추가(정확히 이름이 「감마몬」인 디지몬만, 진화원에 해당 이름 포함 카드가 있을 때).
5. **P-142 (Q4249) — 【등장 시】 두 번째 문장(레이브몬 진화원 아래에 놓고 그 디지몬으로 어택) 수동 처리.** 수정: shard64 스크립트(레스트 상태 레이브몬은 어택 불가).
6. **P-139 (Q4246) / P-144 (Q4259) — 진화원에 "특징 X항체"만 있어도 조건 충족으로 처리.** 공식: 특징이 아니라 「X항체」로 기술된 카드(이름/〈룰〉명칭)만 인정. 수정: `shard43.js` leoSrc, `shard4.js` P-144 noAttack에서 특징 검사 제거(이름 기준 `cardNameIs`).
7. **P-033 (Q4145) — 자신의 턴, DP 13000 이상 블랙 디지몬 전부 《관통》 부여 미구현(HOOK 없음).** 수정: shard64 HOOKS.grantKw(실시간 DP 판정, 재귀 가드).
8. **EX9-056 (Q4813) / P-187 (Q4631/4632) — 비용/효과의 "시큐리티에 놓기"가 수동 확인 프롬프트.** 수정: shard64에서 아군/상대 디지몬(P-187은 디지몬/테이머) 선택 → 소유자 시큐리티 아래(P-187은 위/아래 선택)로 이동 → 상대 시큐리티 위 1장 파기 자동 처리.

등록: `src/cards/index.js`에 shard64 추가. 그 밖의 파일 수정은 위에 적은 `state.js`(optionColorOk, playFreeFromZone 가드, stackHasDP, evolveTargetRestriction), `decktools.js`, `shard4.js`, `shard43.js`의 국소 편집뿐.

## 3. 시나리오 해석 오류로 판명된 것 (엔진은 공식 답과 일치)

- Q4124 P-012: `mainAbilityPayable`은 비용만 본다. 육성 에어리어 브이드라몬으로는 실제 발휘 시 테이머가 레스트되지 않으므로 공식 답과 일치 → 발휘 결과로 검증하도록 변경.
- Q4575/Q4180: 대상 선택이 `required:true`(취소 불가)임을 확인하는 방식으로 변경(하네스가 null을 넘긴 것이 원인).
- Q4289: 하네스가 이미 비어 있는 디지몬을 계속 선택한 것이 원인.
- Q4027: 면역 중 기록된 DP-3000은 15-15-5에 따라 면역이 풀리면 적용됨(Q4306/4307과 일치). 기대값 수정.
- Q4242(Lv.- 디지몬): 데이터에 Lv.- 디지몬이 없어 검증 불가 → 시나리오 철회.

## 4. 확인하지 못한 것 (정직한 한계)

- 분류상 T였지만 시나리오를 만들지 않은 약 790건: 표향 시큐리티 계열(Q4370-4379 등, 구현 여부 미확인), 동시 유발 순서(Q4139/4142/4284/4413 등), 소멸 동시 처리·발휘 대기 중 이탈(Q4285/4286/4324), ≪퇴화≫/≪블래스트 진화≫ 타이밍, 리서치가 필요한 조합 카드, 토큰(Q4127·4222), 링크/아프합체(BT21 계열), 파티션/디코드, 오버플로우·표기 위치 상호작용, 「효과를 받지 않는다」 부가 규칙(Q4305-4308, 4394-4397). 일부는 이미 다른 슬라이스/이전 라운드에서 검증됐을 수 있으나 여기서는 확인하지 않았다.
- 어택 진행 중 새 어택 선언 금지(UI/main.js 계층), 누가 결정하는가(플레이어 선택 UI), 발휘 여부 강제(UI)는 Node 하네스로 검증 불가 → N 또는 미구현.
- 통과한 시나리오 중 일부는 약한 단정이다: P-016(Q4128, 보너스 1 이상 확인), EX9-025(Q4778, 1마리만 변경), EX10-025(Q5079)처럼 "부작용 없음"만 보는 것이 있다.
- 하네스는 `lib-driver`와 같은 방식의 ctx(선택 콜백)로 스크립트를 직접 실행한다. main.js의 실제 유발 큐/UI 경로(`pendingEffect` 대기 등)는 통과하지 않는다. 딜레이는 main.js와 같은 순서(`discardForDelay` → 본문 스크립트)로 재현했다.
- 같은 Q&A를 다른 카드가 그대로 반복하는 항목(dupmap)은 대표 카드 1장만 구현한 경우가 있다(예: Raising 진화 코스트 계열은 11장 전부 구현, 표향 시큐리티 계열은 미구현).
- 다른 에이전트가 동시에 `scripts/qa/lib.mjs`를 덮어써서(원본 손실) 이 슬라이스는 `lib-s4.mjs`를 별도 사용한다.

## 5. 회귀 확인

`node scripts/check-syntax.mjs` OK, `node scripts/audit-effects.mjs` coverage 100.0%, `node scripts/soak.mjs 40` distinct errors 0, `node scripts/test-cpu.mjs` errs 0 / RESULT OK, `node scripts/test-all.mjs` RESULT OK (수정 배치마다 실행, 마지막 실행 결과 동일).

## Round 2 (scripts/qa/qa-slice4-r2-{a..i}.mjs, 공용 하네스 lib5.mjs)

원문은 여전히 `data/rulings/slice4.json`(gitignore)에만 있고, 아래는 Q id와 자체 요약만 적는다.

### 수치

| 항목 | 개수 |
|---|---|
| 1차 미구현 T 항목 (남은 고유 문항 그룹) | 771 Q id / 448 그룹 |
| 2차 신규 시나리오 | 193개 (a 61, b 5, c 32, d 12, e 16, f 26, g 11, h 11, i 19) |
| 2차에 새로 커버한 Q id | 약 250 (동일 답변이 여러 카드에 반복되는 항목은 공용 템플릿 1개가 여러 Q id를 대표: a 81, b 33, c 32, d 12, e 25, f 26, g 11, h 11, i 19) |
| 통과 (최종) | 193 / 193 (1차 a~h 163개도 전부 통과) |
| 실패 → 엔진 수정 | 아래 7건 |
| 실패 → 미해결 | 0 |

주의: "템플릿" 시나리오는 같은 규칙을 카드별로 반복해 물은 Q(예: 표향 시큐리티 14장x4문항, 「효과를 받지 않는다」 7장x4문항)를 공용 상태에서 검증한다. 카드 고유 스크립트까지 전부 통과한 것은 아니며, 카드별 스크립트를 실제로 돌린 것은 링크(f), 비용 전부-또는-없음(g, h), 「그 후」 계열(c, d)뿐이다.

### 커버한 묶음
- a: 표향 시큐리티(놓기/체크/【시큐리티】 유발/셔플로 뒷면화/2번째 카드 뒤집기), 시큐리티 체크 동시 유발 순서(【시큐리티】 효과가 먼저).
- b: 「효과를 받지 않는다」(부여 기록만, 이후 면역/해제 시 반영, 선택은 가능).
- c: ST20/21 연계 계열 8장(연계 부여는 임의 불가, 부여 대상과 어택 대상 분리, 어택 안 함 선택, 조건 불충족이어도 「그 후」 어택).
- d: 「~라면, A. 그 후, B.」에서 A 조건이 안 맞아도 B는 처리(BT12-055, BT15-081, BT16-069, BT17-101/102, BT19-020, BT20-019/102, EX6-062, EX8-072, EX9-021, BT22-077).
- e: 배틀 동시 소멸 시 진화원 효과 불발(10장), 「Lv.이 같은 카드가 2장 이상 겹쳐져」(맨 위 카드 포함), 시큐리티 0장에서 표향 옵션 【메인】(5장).
- f: 링크 카드 파기 비용(어느 링크 카드든, 자기 자신 포함, 10장x2), 〈링크〉 없는 카드는 링크 불가(6장).
- g/h: 비용 부분 지불 불가와 비용 미지불 시 후속 문장 미처리(EX9-049/050/052, BT22-049, BT20-082, BT21-022/062, EX10-034/055/056/058, BT22-010/011/086, P-193, EX9-043/066/068), 【시큐리티】 「앞면이었다면」 4장.
- i: 「【진화 시】 효과는 발휘하지 않는다」(BT20-033, BT22-038), 테이머(등장 턴)에서 진화한 카드의 당턴 어택 불가 4장, [패]【메인】 자신 디지몬 0일 때 사용(4장), 「어택 중이라면」이 상대 어택도 포함(BT20-015/053), EX9-044/BT22-039 자기 자신 등장/진화 시 유발.

### 수정한 엔진 버그
1. **ST20-04 계열 8장 (Q4693-4701, 4731/4732) — 「다른 디지몬이 등장/진화했을 때, 그 디지몬이 특징 X를 가진다면, A. 그 후, B」.** 원인: `state.js parseWatcherTrigger`가 「그 디지몬이 …를 가진다면」 조건을 유발 조건 전체로 취급해, 특징이 없으면 【자신의 턴】 효과 전체가 발동하지 않음. 공식: 조건은 첫 문장(≪연계≫ 부여)만 제한하고 「그 후」 어택은 진행. 수정: 파서가 `condRest`(그 후 이후 문장)를 보관하고 `emitGameEvent`에서 조건 불충족 시 그 부분만 대기열에 넣음(범용).
2. **「…라면, A. 그 후, B」 일반 컴파일 (Q4706/4707/4713/4725/4736/4764/4942 외, BT12-055, BT15-081, EX6-062, BT22-077 등).** 원인: `effects.js`의 조건 문장 컴파일러가 「그 후」 이하를 조건 안에 넣고 오직 ≪세이브≫ 꼬리만 예외 처리(BT19-020). 수정: 첫 문장 이후의 모든 「그 후,」 문장(앞 결과를 참조하지 않는 것)을 무조건 처리.
3. **BT17-102 (Q4713), BT20-102 (Q4725), EX9-021 (Q4764) — 카드 고유 스크립트.** BT17-102/BT20-102는 「그 후」 문장까지 조건으로 묶음, EX9-021 【진화 시】는 컴파일 결과가 빈 스크립트였음(조그레스 진화 여부와 무관하게 아무 일도 안 함). 수정: 새 `src/cards/shard73.js`에서 재작성(EX9-021은 조그레스 시 상대 효과 면역 부여 후 가장 Lv이 높은 상대 디지몬 전부 소멸).
4. **「Lv.이 같은 카드가 2장 이상 겹쳐져 있다」 (Q4879 외 10장: BT22-031/047/048/051/056/063/072/073/077/102).** 원인: 진화원만 세고 맨 위 카드를 제외(`effects.js`, BT22-031, BT22-063). 공식: 겹쳐진 카드 전체(맨 위 포함)에서 같은 Lv 2장. 수정: effects.js 조건 함수, shard73에서 BT22-031/BT22-063 재정의.
5. **링크 카드 파기 비용 (Q4583/5000 계열, 10장x2).** 원인: `trashLink`(shard20)와 `linkCostDiscard`(shard6)가 항상 가장 마지막에 붙은 링크 카드를 파기. 공식: 플레이어가 어느 링크 카드를 파기할지 선택(자기 자신 포함). 수정: shard6 `linkCostDiscard`에 `pickLinkCard` 선택 추가, shard73에서 `trashLink` 연산 재정의.
6. **BT20-015/018/053/056 (Q4715/4716/4721/4724) — 「어택 중이라면」.** 원인: shard13 `attacking()`, shard37 BT20-015가 자신이 어택 중일 때만 참으로 판정. 공식: 상대 어택 중(다른 효과로 진화)에도 충족. 수정: `state.attackCtx` 존재 여부만 검사.
7. (1번과 동일 원인의 부수 효과) `[턴 1회]`는 조건 불충족 후속 처리에서도 소비됨 — 공식 답변과 일치하므로 그대로 둠.

### 해석상 정상이었던 것 (엔진 수정 없음)
표향 시큐리티 전부(Q4371-4379 계열, 5031-5034 계열 등 14장), 「효과를 받지 않는다」 계열, 배틀 동시 소멸 시 진화원 효과 불발, 비용 부분 지불 불가 계열, 【진화 시】 봉인 계열은 이미 공식 답과 일치했다.

### 미해결
없음 (실패 항목은 모두 위 수정으로 통과).

### 확인하지 못한 것 (정직한 한계)
- 2차에서 만들지 않은 고유 문항 그룹 약 250개(약 520 Q id 중 반복 제외 기준): 진화 선언 단계 UI 규칙(BT4-011 계열 28 Q, 「진화하지 않음을 선택할 수 없다」), 【登場時】 봉인 상세(Q4348-4354 BT20-037), 「효과를 받지 않는다」에 트리거 효과 부여 후 유발(Q4308 계열 7장, 4561/4582/4627/5014 등), 링크 상한/링크 옮기기(Q4558/4578/4599), 디지크로스·조그레스 개별 규칙(Q4184, 4299 계열의 실제 조그레스 끼어들기), 어택 종료 효과(Q4729 계열), 테이머 아래 카드 놓는 순서(Q4522/4607/5119 등), EX9-073/EX10-059의 진화원 효과 타이밍, BT22-007 육성 에어리어 DP 처리(Q4864/4865).
- 〔턴 1회〕 소비 여부, 유발 순서 선택(Q4139/4142/4273/4345/4401 등 「플레이어가 순서 선택」)은 순서 선택 UI가 필요해 Node 하네스로 단정하지 못했다.
- 템플릿 시나리오는 대표 규칙 검증이며 카드 개별 문구 차이(예: 표향 시큐리티 카드의 개별 【메인】 효과)는 검증하지 않았다.
- 1차 한계와 동일하게 main.js의 실제 발휘 대기열/UI 경로는 통과하지 않는다.

### 회귀 확인
`check-syntax` OK, 1차 a~h 및 2차 a~i 전부 통과, `audit-effects` coverage 100.0%, `soak 40` distinct errors 0, `test-cpu` RESULT OK, `test-all` ALL PASSED (21 steps), slice5 link/face, slice6-e 회귀 통과.
