# Unresolved engine problems (batch A) - 2026-09

Source: the "Unresolved" sections of docs/audit-2026-09-full-recheck2-w4/w6/w9.md. Official Q&A (data/rulings/all.json, ids below) is authoritative.
Regression scripts: `scripts/qa/qa-unres-a-*.mjs` (each prints `pass N, fail 0`).

## Fixed

| # | Problem | Rulings | Fix | Script |
|---|---------|---------|-----|--------|
| 1 | EX2-007 (Digi-Egg with DP in the battle area) was excluded from generic 「디지몬」 pickers/filters; a returned Egg raised the hand/security | 1198, 1265, 1270, 1272, 2402, 3280, 3281, 3284, 3285, 3558 | `state.js`: `isBattleEgg` / `isDigimonLike`; `isAsDigimon` now includes a battle-area Egg with DP (so `effectiveInfo(...).isCategory('digimon')` and `category:'digimon'` filters match). `effects.js`: destroy / setDP / battle-immunity / skipUnsuspend / restrict / per-opponent-digimon counters use `S.isDigimonLike`. All shard-local `isDig`/`isDigimon`/`digs` helpers (~40 shard files) now delegate to `S.isDigimonLike`. Redirect (3-1-3-9): `emitGameEvent` and `fxEmit` run `normalizeDigitamaZones` first, so an Egg sent to hand/deck/security ends on the digi-egg deck bottom and 「늘어났을 때」 (handIncrease / securityIncrease / deckIncrease) does not fire; the "cost" of 「…패로 되돌리는 것으로」 still counts as paid. | qa-unres-a-ex2007 |
| 2 | LM-019 보코몬: one activation must protect ALL simultaneously leaving digimon | 4002, 4268 | `state.js`: `trySurviveByPrintedAbility` records the used group ability (`state._prGrp`, reset by `beginCause`); later qualifying digimon of the same cause are covered without paying again. `orderSimulDelete` keeps a holder of such an ability last (`holdsGroupSurvive`) so it is still on the field while its group is processed. | qa-unres-a-lm019 |
| 3 | BT8-095 ≪아머 퍼지≫: remaining ≪S 어택≫ checks must continue after the attacker survives a lost security battle | 4705 | `state.js`: new `settleSecurityLoss(ctl)` (deletes the loser now, honours battle immunity, returns `survived` and resets `ctl.done` when a replacement kept it) + `secLossAfter`; interactive replacement prompt: `ctl.onLossResolved(r)` called from `resumeReplacement`. Callers switched from a raw post-loop `deleteStack` to the settle loop: `cpusim.js`, `main.js` (`doSecurityStep`, `scriptedSecurityCheck`), `scripts/fuzz.mjs`, `qa/lib-s1.mjs`, `qa/lib-s6.mjs`; `resolveSecurityCheck(..., {settle:true})` used by `qa/lib2.mjs` and `soak.mjs`. | qa-unres-a-bt8095-armorpurge |
| 4 | BT25-075: when the link cap drops, the PLAYER chooses which link card is discarded | 6370 | `state.js` `ruleCheckDP`: an excess with differing link cards queues a mandatory pending choice (`__링크상한`, op `ua_linkTrim` in shard52, uses the existing `pickLinkCard` UI/CPU prompt); identical cards keep the old automatic discard. | qa-unres-a-bt25075-linkcap |
| 5 | EX7-014 / EX7-049 as DigiXros materials: their play option was never offered | 6718, 6719 | `state.js` `placeXrosMaterials`: `offerXrosLeaveOptions` collects the passive `preventLeaveOptions` of the leaving material and queues `__이탈효과` (op `ua_leaveOpt`, shard52) so the player can use it; `shard4.js` options got a `label`. | qa-unres-a-ex7-xros-leave |
| 6 | Forced/borrowed 【등장 시】 vs the BT20-037 suppression flag | 4350, 4351, 4352, 4353 | `state.js`: `borrowerPlayTrigBlocked`. Applied to EX5-059/061 (`runTagOf`), BT11-029 + BT10-112-style borrowers (`runCardSegments`, `s2_runTamerEffect`), BT15-102, BT18-066 (`s4_placeAndBorrow`), EX9-073, BT23-060, BT23-101 (`shard6.js`), EX3-065 (the evolved digimon's flag, `shard39.js`). Per 4353 the 「것으로」 cost is not paid either (checked before the cost). The flag on the card whose effect is borrowed is ignored (4352). `qa-w7r2-a` Q4135 updated accordingly (card no longer placed while locked). | qa-unres-a-forced-play-trig |

Test-helper fixes made along the way: `qa/lib-s6.mjs` `plainTamer` falls back to a synthetic vanilla tamer (the current card DB has none, 4 unrelated scripts crashed); `qa-slice2-tpl` / `qa-slice2-r2-family` exclude Digi-Egg cards from the fabricated deck (an Egg in hand is now moved to the digi-egg deck by rule 3-1-3-9).

## Still open / caveats

- LM-019 group coverage applies to the ability-per-cause model (same cause = one effect resolution / one battle / one rule check, like the existing hook group coverage); wipes that delete digimon through several separate `deleteStack` calls outside `orderSimulDelete` (e.g. some rule-check DP-0 sweeps) do not reorder the holder to the end.
- BT8-095 in the browser: a scripted (`scriptedSecurityCheck`, ≪관통≫ bonus) check that parks an interactive replacement prompt does not resume the remaining checks after the prompt (the normal attack flow does).
- EX2-007: mass conversion of `isDig`-style helpers covers the shard helpers that take stacks; id-based helpers (`isDig(id)` in shard6/7/14) are unchanged (they cannot see the zone).
- Not headless-provable: the UI rendering of the new pending prompts (`pickLinkCard`, DigiXros leave option) - logic is covered, the modal flow uses existing choice kinds.
