# 룰북 Ver.4.2 6장(게임 진행) / 7장(등장·디지크로스·어셈블리) 대조

방법: 룰을 한 조항씩 읽고 코드(state.js / engine.js / main.js) 확인 + Node에서 실제 상태로 실행.
상태: **수정됨** / **열림** / **정상**(검증 완료).

| rule no. | rule content | engine behavior | status |
|---|---|---|---|
| 6-1-2 | 페이즈 순서: 액티브→드로우→육성→메인 | PHASE_ORDER 그대로 | 정상 |
| 6-1-3 | 해결할 처리가 없어질 때까지 다음 페이즈로 진행하지 않음 | autoAdvance는 pending 있으면 정지하지만, "다음 페이즈" 버튼·부화/이동 클릭은 pending/선택창이 남아도 진행 가능했음 | 수정됨 (main.js 버튼/부화/이동에 blockIfBusy) |
| 6-1-4-1 | 메모리가 상대측 1 이상 + 해결할 처리 없음 → 그 페이즈 그대로 턴 종료 | checkAutoEndTurn이 대기 중 효과(【등장 시】 등) 유무와 무관하게 즉시 턴 종료 → 효과가 상대 턴에 해결됨 (메모리를 되돌리는 등장시 효과가 무의미해짐) | 수정됨 (checkAutoEndTurn이 미해결 pending 있으면 보류; main.js render가 비면 재검사) |
| 6-1-4-1 | 턴 종료 조건은 메인 이외의 페이즈에서도 성립 | 메인 페이즈에서만 검사 | 수정됨 (autoAdvance가 비-메인 페이즈에서도 검사) |
| 6-2-1 | 턴 시작 시 "에리어의 자신의 카드 전부" 액티브 | 배틀 에어리어만 액티브, 육성 에어리어 카드는 제외 | 수정됨 (engine.nextPhase에 raising 포함) |
| 6-2-1-1 | 턴 시작 시 처리는 액티브보다 먼저 | queueTurnStartTriggers + autoAdvance가 pending에서 정지 | 정상 |
| 6-2-1-2 | 턴 시작 처리 후 종료 조건 충족 시 액티브 전에 턴 종료 | 검사 없이 unsuspend 진행 | 수정됨 (위 autoAdvance 검사, 테스트: 메모리 2로 p2 턴 시작 → p1 턴으로 넘어감) |
| 6-3-1 / 6-3-1-1 | 드로우 1장, 선공 첫 턴만 드로우 없음 | turnNumber===1 && firstPlayer 조건 | 정상 |
| 6-4-1 | 육성 페이즈 행동 택1: 부화/이동/없음 | breedingActionTaken로 1회 제한, UI 활성 조건 일치 | 정상 |
| 6-5-1 | 메인 페이즈 행동은 해결 안 된 처리가 없을 때만 | 등장/진화/조그레스/옵션/어택/패스/메인효과는 blockIfBusy. **링크(뱃지 드롭)에는 누락** | 수정됨 (링크 드롭에 blockIfBusy) |
| 6-5-1-1-1 | 손패 디지몬/테이머 등장 (코스트 지불) | playFreshFromDrag → spendMemory → playDigimonFresh | 정상 |
| 6-5-1-4 | "손패/배틀 에어리어의 카드" 링크 | UI는 손패에서의 링크만 지원 (linkCardTo의 source 인자는 있으나 배틀 에어리어 카드를 옮기는 UI 없음) | 수정됨 (S.linkFromBattle + UI 드롭) |
| 6-5-1-7-1 | 패스: 즉시 메모리 상대측 3 | declarePass: p1 -3 / p2 +3 고정 | 정상 |
| 6-6-1 / 6-6-2 / 6-6-3 | 턴 종료 시(【턴 종료 시】) 효과는 아직 그 플레이어 턴·그 페이즈에서 해결한 뒤 턴 종료 | endTurn이 종료시 효과를 queue한 **직후** activePlayer/turnNumber를 넘김. 【자신의 턴 종료 시】 pending이 상대 턴 unsuspend 상태에서 해결됨(테스트 확인: active=p2인데 pending=p1 턴종료시) → "이 턴 동안" 지속시간·메모리 재검사가 어긋남 | 수정됨 (engine `beginTurnEnd`→pending 해결→`settleTurnEnd` 2단계. 종료 시 효과는 activePlayer/turnNumber가 아직 종료 중인 플레이어인 상태에서 해결, 만료(clearExpiredModifiers)는 확정 시점. soak/main.js 갱신) |
| 6-6-4 | 턴 종료 시에 메모리가 0 이상(자기측)이 되면 턴 종료 안 됨 | viaMemoryCondition 재검사 있음. 단 위 구조 문제로 종료시 【효과】로 인한 메모리 복귀는 반영 못함(runEndOfTurnEffects 예약함수분만 반영). 또한 취소 시점에 이미 clearExpiredModifiers가 실행됨 | 수정됨 (`settleTurnEnd`가 pending 해결 후 재검사하여 메모리가 돌아왔으면 만료 처리 전에 취소, 페이즈 유지) |
| 7-1-2-1 | 등장한 카드는 그 턴 어택 불가 | attackEligibleTurn = turn+1 | 정상 |
| 7-1-2-2 | 등장 시 즉시형 효과는 카드 공개 직후 | 「등장할 때」 지불 훅은 UI confirm으로 처리, 별도 타이밍 구분 없음 | 열림 (단순화; 실사용 카드 영향 미미) |
| 7-1-2-3 | 공개 후 등장 불가 시 카드 복귀, 코스트 미지불 시 메모리 불변 | 등장 제한(DP)은 지불 전에 검사 → 메모리 미변동 | 정상 |
| 7-1-3 | 절차: 공개→코스트→배치 | 코스트 지불→배치→【등장 시】 queue | 정상 |
| 7-2-2-1 | 아래에 놓은 카드 1장당 지정 수치만큼 등장 코스트 감소 | discount = per × 재료 수 (0 미만 방지) | 정상 |
| 7-2-2-2 | 디지크로스 선언은 등장 시 효과 이후, 코스트 지불 직전 | confirm 후 코스트 계산 → 지불 → 배치 | 정상 |
| 7-2-2-3 / 7-2-2-4 | 재료를 선택(조건 수까지 원하는 장수, 최소 1장) | planDigiXros가 손패→배틀 순으로 **탐욕적으로 최대한** 자동 선택하고 confirm(예/아니오)만 제공: 일부만 선택하거나 어떤 카드를 쓸지 고를 수 없음 | 수정됨 (digiXrosOptions/planDigiXrosPicked: 후보를 하나씩 선택(최소 1장)) |
| 7-2-2-5 | 조건에 적힌 카드는 각 1장씩(장수 지정 시 그 장수까지) | reqs[].count 반영, distinct 처리 | 정상 |
| 7-2-2-6 | 손패 재료는 선언과 함께 공개 | 공개 처리 없음 (연습용 전체 공개라 무의미) | 의도적 단순화 |
| 7-2-2-7 | 배틀 에어리어의 재료 카드는 그 시점에 배틀 에어리어를 벗어남 | splice로 제거하고 진화원째 이동. 떠남 관련 트리거(소멸 아님, "배틀 에어리어를 벗어날 때")·링크 카드 정리·hookLeave 미호출 | 수정됨 (placeXrosMaterials가 링크 카드 정리 및 hookLeaveTriggers(cause "xros") 호출) |
| 7-2-2-8 | 겹침 순서: 조건 **왼쪽**에 적힌 카드가 위 | 재료를 조건 순서대로 sources 뒤에 push → 왼쪽 카드가 가장 **아래**였음 | 수정됨 (재료를 역순으로 push; 테스트: 샤우트몬X4 top=샤우트몬) |
| 7-2-2-9 / 7-2-2-10 | 실제로 X장이 놓였을 때만 X장 디지크로스했다고 간주 | 기록 자체가 없음 (효과 텍스트 "디지크로스하고 있었다면/2장 디지크로스하고 있었다면" — BT10-024/061/066, BT11-009, BT11-086 등 조건 판정 불가). 이번에 stack.xrosCount 기록만 추가 | 수정됨 (xrosCount = 실제로 놓인 재료 수; 조건 "N장 디지크로스하고 있었다면"을 effects.js가 파싱, 조건절 미평가로 무조건 실행되던 컴파일 오류도 수정) |
| 7-2-2-11 | 절차 중 등장 불가가 되면 아래 카드 트래시 | 등장 불가 검사는 재료 이동 전에 하므로 발생 안 함 | 정상 |
| 7-2-2-12 | 디지크로스는 강제 아님 | confirm 거부 가능 | 정상 |
| 7-2-2-13 | 효과로 등장시킬 때도 디지크로스 가능 ("이 효과로 …" 카드 포함) | playFreeFromZone/효과 등장에는 디지크로스 재료 선택 없음 (BT10-104의 트래시 재료 등도 미지원) | 수정됨 (effects.js playFree가 디지크로스 재료 선택을 제공(playFreeFromZone opts.materials)) |
| 7-2-3 | 절차(공개→효과→선언→재료→코스트→배치) | 7-2-2-2 참조 | 정상 |
| 7-3 (아셈블리) | 트래시의 카드 N장을 아래에 놓고 코스트 감소 | 코드/UI 전무 (parse도 없음). 해당 카드는 일반 등장만 가능 | 수정됨 (S.parseAssembly/solveAssembly/planAssembly(35장 파싱) + 드롭 UI; 트래시에서 정확한 매수 필요(전부 아니면 불가)) |

## 검증
- `node scripts/soak.mjs 30` distinct errors 0, `node scripts/audit-effects.mjs` 커버리지 100%.
- 스크래치 테스트: 턴 시작 시 메모리 초과 → 턴 종료, 육성 에어리어 레스트 → 액티브, 샤우트몬X4 디지크로스 겹침 순서.
